# Feature Coverage Map

This map links the app's major product and operational flows to their current
tests, docs, performance checks, and owning areas. Use it before adding new
validation, opening a release checklist, or generating ClickUp follow-up work.
Use `docs/maintainer-operations.md` when you need the public-safe operations
landing page that routes these flows to validation, release, evidence, and PR
handoff guidance.

## Coverage strength

**869f135u5.** These three words used to be defined by prose that did not
discriminate: on the parseable rows, *every* one had a non-empty
Perf/release/docs cell and nearly every one named a spec, so the written test for
Strong was satisfied by almost the whole table while only 11 rows claimed it. A
legend 30 rows pass and 11 rows claim is not a legend.

They are now derived from four facts a script resolves, and
`npm run docs:coverage-grades` fails when a declared grade and its evidence
disagree:

| Axis | True when the cell… |
| --- | --- |
| **unit** | names a `.spec.ts`/`.spec.mjs` that exists, **or** an npm script that exists |
| **browser** | three-valued — `dedicated` (a spec under `e2e/` that is not the smoke pack), `smoke` (the smoke pack or one of its routes), `none` |
| **gate** | names an npm script that exists |
| **evidence** | resolves to a real file |

- **Strong** — `unit` **and** `gate` **and** `browser = dedicated`.
- **Covered** — `unit` **and** (`gate` **or** `browser ≠ none`).
- **Partial** — anything else.

**A flow with no browser surface cannot be Strong, and that is deliberate.**
Strong means *proved through the product, end to end*. A tooling flow that is
fully guarded is **Covered**, and Covered is not a criticism — it is the
difference between "every check passes" and "somebody drove it".

The `browser` axis is three-valued for the same reason. As a boolean, Strong and
Covered collapsed into each other, which is how the old legend stopped meaning
anything.

**Partial has zero rows today.** That is a fact about this tree rather than a
broken criterion: every flow in the table names a unit spec or a script, and a
gate. The grade is kept as a **tripwire** — a row that later loses one of those
lands there and the lane says so — which is the same shape as the provenance
marker in 869f135rg, and for the same reason: a state with no instances is worth
keeping when something actively holds it empty.

Current distribution: **8 Strong, 24 Covered, 0 Partial**.

### A `|` in a cell must be written `\|`

The lane also fails a row that does not split into exactly nine cells. That is not
hypothetical. Line 26 shipped with **19** unescaped `|` characters — regex
alternations like `(Boosts|Increases)` and `enem(y|ies)` inside code spans and
quoted phrases — so GitHub rendered its grade as the word **"every"** and dropped
`Strong` entirely. It is the only part of this task a reader could see.

## Flow Map

| Flow | Product surface | Source modules | Unit/contract coverage | E2E/live coverage | Perf/release/docs checks | Evidence/docs | Owning area | Coverage strength |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Character catalog, favorites, and import feedback in both directions | `/tabs/characters` | `src/app/pages/characters/characters.page.ts` (includes the **Recently viewed** strip of 869f1327d, which is the first consumer `recentCharacterIds` has ever had, and the **region marker** of 869f1327j, rendered only for `out-of-region` so it is absent rather than guessing); `src/app/pages/characters/characters-favorites.utils.ts`; `src/app/core/services/optcbx-import.service.ts` (869f26084. The import always computed a diff and only ever reported HALF of it. Every path that grows the favourites list is additive - `mergeFavoriteIds` unions - so the list only ever grew, and the staleness that accumulated was units the reader no longer owns, which re-importing could not fix. `buildMergeImportResult` now also returns `removableIds`, and a favourite qualifies only when this import did not mention it AND it still resolves to a character: *"this import did not mention it"* and *"we could not recognise it"* look identical in a count and mean opposite things, so an unresolvable favourite is counted in `unresolvedFavoriteCount` and never offered. Removal is opt-in, arrives with nothing ticked, and `removeFavoriteIds` clamps to what was offered so a caller cannot delete something that was never on screen); `src/app/shared/character-tag-filter/character-tag-filter.component.ts`; `src/app/core/services/optc-repository.service.ts`; `scripts/perf-route-load.mjs` | `src/app/pages/characters/characters.page.spec.ts` (includes the character-tag filter narrowing and reset cases, and the 320px column and rail-label wrap rules); `src/app/shared/ability-filter-rail/ability-filter-rail.component.spec.ts` (the shared rail sizes its own columns, down to one); `src/app/pages/characters/characters-favorites.utils.spec.ts`; `src/app/core/services/character-region.utils.spec.ts` (the three region states, and the SQL clause compared against the in-memory predicate on a real SQLite engine); `src/app/core/services/optc-repository.service.spec.ts` | `e2e/smoke.spec.ts` covers the route and side-menu navigation; `perf:route-load` covers deterministic Characters search cold-start timing and route chunk size | `npm run test:ci`; `npm run test:e2e:chromium`; `npm run perf:route-load`; `npm run perf:budget-report`; `npm run build` for shared UI changes | `README.md`; `docs/maintainer-validation-guide.md` | App catalog, unit search, route load, and data runtime | Covered |
| Character detail, edit, and local overrides | `/characters/:id`; `/characters/:id/edit` | `src/app/pages/character-detail/character-detail.page.ts` (carries the same **region marker** as the catalogue card, 869f1327j; its one solid action is the **bridge** from `character-detail-bridge.utils.ts` - a character with a Captain Ability opens Captain Coverage as `?captain=<id>`, any other opens Auto Team Builder - with favourite outlined and the edit/export/import/reset tools behind a toggle that stays open while the character has local changes, 869f13c5c); `src/app/pages/character-edit/character-edit.page.ts`; `src/app/pages/character-detail/character-overrides-transfer.utils.ts`; `src/app/core/services/character-overrides.utils.ts` (an override is merged over the dataset's detail, and keeps the dataset's Captain boosts and tiers while the captain text is unchanged - the repository supplies that text for list rows) | `src/app/pages/character-detail/character-detail.page.spec.ts`; `src/app/pages/character-detail/character-detail-bridge.utils.spec.ts` (the bridge for one character of each of the four shapes, and the one-solid-button template); `src/app/pages/character-detail/character-detail-english-text.spec.ts` (every paragraph that prints the community database's English verbatim carries `lang="en"`, and no translated one does, 869f13c59); `src/app/pages/character-detail/character-detail-saved-team-count.spec.ts` (the line saying how many of the reader's saved teams use the character: teams, not slots, and hidden at 0, 869f13c8r); `src/app/pages/character-detail/character-detail.presenter.spec.ts`; `src/app/pages/character-edit/character-edit.page.spec.ts`; `src/app/pages/character-detail/character-overrides-transfer.utils.spec.ts`; `src/app/core/services/character-overrides.utils.spec.ts` (negative stats survive an editor save; blank fields read as unknown); `src/app/pages/character-edit/character-edit.untouched-save.spec.ts` (the real page save and repository read-back leave a shipped character exactly as it was - its Captain tiers, a trained `6+` rarity label, its Captain boosts, a plain-text Captain Shift, a VS unit with no classes - and so does export -> import; an edited captain text re-reads the boosts and drops the tiers, 869f63gqd) | Detail/edit routes are not in the default smoke route list; use focused live checks for UI regressions | `npm run test:ci`; `npm run docs:integrity -- --brain-root ../optc-team-builder-brain` when override docs change | `docs/data-schemas.md`; `docs/maintainer-validation-guide.md` | Character detail and local override ownership | Covered |
| Manual Team Builder | `/tabs/manual-team-builder` | `src/app/pages/manual-team-builder/manual-team-builder.page.ts`; `src/app/core/services/character-party-conflict-keys.utils.ts` (the name-derived duplicate-character rule the four sub slots share with Captain Coverage and the Auto Team Builder engine); `src/app/core/services/team-coverage-summary.utils.ts`; `src/app/shared/team-coverage-summary/team-coverage-summary.component.ts`; `src/app/shared/character-ability-groups/character-ability-groups.component.html`; `src/app/core/services/character-tag-set.utils.ts` (the picker tag index is built by the shared `buildCharacterTagMatchIndex` helper) | `src/app/pages/manual-team-builder/manual-team-builder.page.spec.ts` (includes the sub-slot duplicate-character rule: assign, drag-copy, Friend Captain swap, and the validation for a team that arrives with a repeat); `src/app/core/services/team-coverage-summary.utils.spec.ts`; `npm run test:i18n-regression` covers critical validation and share-import EN/EL copy; the picker's Clear filters also clears the search, which counts as an active filter; the status line under the builder header, the first-visit intro card (hidden on a saved-team or share-link handoff), and a team with no Friend Captain reading as complete with no missing-leader warning; the picker moving on to the next empty slot after a pick into an empty one (Captain, Sub 1-4, Friend Captain last; a replacement or the last pick closes it, a refused pick stays put, and a drop onto the picker's own empty slot counts as a pick) via `resolveNextManualTeamSlotIndex`, the picker renaming its dialog and focusing its title as it moves on, role labels on the slot cards, the picker rail and the picker title, and the quick-start shortcuts on an empty team | `e2e/smoke.spec.ts` covers route render; `e2e/regression-flows.spec.ts` covers share-code import into the manual builder; `e2e/accessibility.spec.ts` covers share-link hydration, targeted axe checks, and explicit slot actions without nested interactive cards | `npm run test:ci`; `npm run test:e2e:chromium`; `npm run i18n:validate`; `npm run test:i18n-regression`; `docs/copy-review-checklist.md` for copy changes | Public team-building guides in `README.md`; `docs/maintainer-validation-guide.md`; `docs/copy-review-checklist.md` | Manual builder UI and team summary | Strong |
| Auto Team Builder search, requirements, guided build, the enemy-mechanic checklist, the borrowed-leader alternatives, and the alternative Captain | `/tabs/auto-team-builder` | `src/app/core/services/auto-team-builder-captain-feasibility.utils.ts` (869f333ey, issue #523. When a pinned Captain PROVABLY cannot lead the crew asked for - no covered character carries a required type, no covered character satisfies a requirement group, or fewer than four are covered - the search tries up to eight Captains from the same candidate pool that pass the same proof, as ONE exact attempt before anything is relaxed, and reports the swap as the `replacedCaptain` relaxation: axis 12, the Captain row of the Final team report, the debug report. Guided mode rejects it and offers the Captain in one click. A Captain that can lead the crew never reaches this, and a pinned Friend Captain keeps its seat. The same change memoises the leader-scope answers the sub search asked at every leaf - 186 s to 3.6 s for the report request with no Captain pinned, same team); `src/app/core/data/ability-tag-glossary.data.ts` (869f2608x. What each ability term MEANS, in the player's words, shown on the picker tile beside the match count rather than in a glossary screen - a definition the reader has to go and look up is one they will not. Keyed by ability-tag key rather than by screen, which is why it is a TypeScript constant like `whats-new.data.ts` and not a transloco scope: a per-screen scope would have to mirror 263 tag keys and could drift from them silently. Scope set by measurement - the terms are ranked by how many of the 4,618 characters carry each, and `GLOSSARY_REQUIRED_TOP_TAG_COUNT` makes the most-met ones fail the `ability-tags` lane when one loses its entry. The 22 tags no shipped character carries need no copy at all); `src/app/core/services/auto-team-builder.utils.ts` `buildFriendCaptainAlternatives` (869f2608f. `resolveLeaderCandidateOptions` already sorted and filtered the friend-captain pool and the engine discarded it once a pair succeeded; the result now keeps it. **No second search runs** - the premise the idea was gated on, N searches at 31,744 attempts each, did not apply, because `allowAnyFriendCaptainAutoFill` already widens the leader pool with the whole roster inside ONE run. The list claims only that each leader met the search's own leader rules, never that it would produce a legal team: the loop returns on the first success, so every alternative was RANKED and not TRIED. It is deliberately not filtered by the reader's box, because a borrowed leader is never constrained by what they own);  `src/app/core/services/auto-team-builder-mechanic-checklist.utils.ts` (869f1935z's three-state checklist, whose written-out count of unanswerable mechanics is now guarded against the catalogue by the `enemy-vocabulary` lane of 869f1328q - it said fourteen while the catalogue held **fifteen**);  `src/app/pages/auto-team-builder/boosted-characters-transfer.utils.ts` + the `boostedCharacterIds` list on `UserStateService` (the reader's own boosted-unit list for the event they are playing, hand-entered because there is **no event or boost data in the dataset at all** and a boost list is event-scoped and time-bound - a shipped one would be stale within days. It steers **ranking, never legality**, in both seat kinds. For subs, `BOOSTED_CHARACTER_SCORE` is one point inside `resolveSubSelectedFilterScore`, the same small scale as the counts beside it, so a boosted character wins among equals and never outranks a character the reader's own filters match better. For leaders it is a comparator **step** in `compareAutoFillLeaderCandidates` rather than a score term - the surrounding code is a sequence of tiebreaks, not a sum - placed **after** the reader's preferred leaders and their ability-requirement coverage and **before** `compareCandidatesByNewestId`. That position is the design: a boost beats the arbitrary newest-id fallback, which answers nothing the reader said, and loses to everything they actually asked for. Pinned by mutation in both directions - moving the step above requirements or below the id fallback each turns tests red - 869f1rmuu. Deliberately **not** carried in a preset export - a preset is a reusable setup and a boost list is true for one week. Durable user data, so the storage-keys guard required an export scope before the key could ship - 869f12xa5); `src/app/core/services/character-mode-effects.utils.ts` + `src/app/pages/auto-team-builder/team-mode-effects.utils.ts` (which members of the built team behave differently on a Treasure Map or in Kizuna Clash, read from the shipped `specialText` - **60** and **12** characters respectively, no overlap, measured over all 4,618 rows of `sourceVersion` 36. Only `specialText` carries these clauses, not captain abilities, sailors or supports. Two phrasings that mean different things: a **conditional** effect that exists only in that mode, and a **scaling** magnitude that depends on the reader's level in it; 47 carry both. It **does not change what the search picks** and the card says so - there is no event or boost data in the dataset at all, so boost weighting stays a hand-entered list and the owner's call - 869f12xa5); `src/app/pages/auto-team-builder/special-charge-timeline.utils.ts` (when each special on the built team is charged, against a run length the reader enters - there is no stage table, so the turn count cannot come from the dataset. Reports turns to CHARGE and never "ready on turn N": nothing in this app states how a cooldown maps onto a turn index, so an off-by-one would be wrong on every row and invisible. `special_cooldown_max`/`min` have 97.62% coverage with zero inversions, which is what settles max=untrained / min=fully-levelled; the 110 gaps are named rather than dropped. Limit Break lowers cooldown further and is not in the dataset, which the card says - 869f12xbm); `src/app/pages/auto-team-builder/auto-team-builder-result-snapshot.utils.ts` (the built team, parked in Preferences so a reload does not throw it away - 869f12xbc measured a completed build writing **zero bytes** of storage. Stores character IDS and re-reads the records, which keeps a recomputable cache small and stops a restored team going stale across a dataset release; a missing id abandons the whole snapshot rather than showing a team the builder never produced. Forgetting is an intent, never a side effect of the reset that runs on every page load); `src/app/pages/auto-team-builder/auto-team-builder.page.ts` (the requirement-source, manual-lock, and exclude candidate pickers each host the shared `app-character-tag-filter`; all three are query-backed, so the resolved ids ride the existing `allowedCharacterIds` channel into `searchDetailedCharacters` and the tag gate is applied inside the query, before `LIMIT`/`OFFSET` — a filter change restarts paging at offset 0 and "load more" keeps carrying the gate); `src/app/core/services/auto-team-builder.engine.ts`; `src/app/core/services/auto-team-builder.worker.ts`; `src/app/shared/ability-requirement-picker/ability-requirement-picker.component.ts`; `src/app/core/services/ability-requirement-draft.utils.ts` (ability requirement chips/visuals; `change_block_slots` answers "can this effect touch [BLOCK] at all", since a [BLOCK] orb "cannot be changed by most specials unless they say so specifically" — which is why its verb-less `including [BLOCK]` pattern is upstream's block-immunity PIERCE marker rather than a loose substring (across all 586 upstream uses it never points at a [BLOCK] destination, and the glossary's own named example of a special that can change block orbs, Jerry Cipher Pol No. 6 #722, reaches the key through exactly that phrase); it also matches the two clearing wordings that never say "changes" — a directed randomize ("Randomizes [BLOCK] orbs into either [QCK] or [DEX] orbs") and the "turns" verb — while "[BLOCK] orbs not reduce damage" is penalty removal and stays out; the key deliberately spans BOTH directions (clearing and creating [BLOCK], e.g. Pell #3658 trading his own orb for cooldown), mirroring Fandom's Block Slot Manipulator ("can change orbs to [BLOCK] or from [BLOCK] to another orb") — hence the neutral label; note it is NOT a subset of `change_slots` (which excludes randomizers by design), unlike Fandom's taxonomy where Block Slot Manipulator ⊂ Slot Change; `remove_enemy_end_of_turn_heal` keys on the ENEMY buff name "End of Turn Heal" with a word boundary, because the crew's own regen buff is spelled "End of Turn Heal**ing**" and characters that CONSUME it ("If your crew has End of Turn Healing … reduces End of Turn Healing duration by N turns" — Garp #4239/#4240, Hibari #4523, Zoro VS Nusjuro #4529) are not stripping anything from an enemy; the possessive cannot be used at this layer because `normalizeTargetText` deliberately strips "enemies'" before any alias sees the target, so the spelling is the only available signal (14/14 enemy strips vs 4/4 crew consumers, clean); the same boundary also drops Garp & Tashigi #4553, whose removal target bridges into a later "increases duration of any End of Turn Healing buffs" EXTENDER clause; `heal_hp` covers every crew HP-recovery wording, including the two that never name HP — the RCV-scaled Post Turn Heal whose "in HP" tail upstream often omits ("recovers 1x character's RCV at the end of each turn", Nami #2675/Tsuru #1319/Rayleigh #1619) and the damage-taken heal ("Recovers 50% of damage taken from enemies", Katakuri #2364/Magellan #3277, which `reduce_damage` correctly rejects since it is a heal rather than a reduction); requiring an HP token hid 77 characters, the same failure shape as the decimal-stop bug via a different tail, and the boosts-guard keeps the RCV alternative from reaching a neighbouring "boosts … RCV" (`boost_rcv`) clause; ENEMY self-heal ("Fully heals", boss mechanics) must never be tagged here; buff DURATIONS are selectable for the audited duration-bearing matcher keys (`DURATION_TURN_KEYS`: boost_atk, boost_slot_effects, reduce_damage, reduce_damage_over_threshold, nullify_damage) — the turn count is read from the matched clause's OWN window (from the clause end to the next effect verb), never from the whole sentence, because specialText is multi-effect and a max-over-text reading is wrong for 250 of boost_atk's 1,359 durationed characters (Usopp #572 "Boosts ATK … by 2x **for 1 turn**, binds himself **for 15 turns**" would report a 15-turn ATK boost); a range records its guaranteed minimum ("for 1-6 turns" => 1), matching the convention `TURN_PATTERNS` uses for "reduces … by 1-5 turns", and "for 0-6 turns" is therefore dropped by the > 0 guard; `reduce_special_charge`/`reduce_ship_special_charge` are deliberately excluded because their "by N turns" is the AMOUNT of cooldown removed at the start of the fight, not a duration; captain-sourced `reduce_damage` correctly has no duration (509 of its 519 clauses are always-on passives); `boost_slot_effects` matches BOTH grant forms of the same Orb Effects stat — the multiply-by form scoped by character ("boosts Orb Effects of [INT] characters **by** 2.75x") and the set-to form scoped by orb type ("increases Orb Effects of beneficial [TND] orbs **to** 2.5x"), the latter having left 21 characters invisible to the filter; the amplifier guard is future-proofing only, since upstream names that buff "Orb Amplification" rather than "Orb Effects"; damage-reduction taxonomy — `reduce_damage` is the umbrella ("reduces damage received/taken by N%", with `recieved`/`take` tolerating upstream typos and a threshold form that drops "received"), and its two subsets were both DEAD keys revived by the 2026-07-16 audit: `reduce_damage_over_threshold` = "Reduce Damage Above A Threshold (Threshold Damage Reduction)" demanded "over … HP" but OPTC-DB only ever writes "reduces any damage received **above** 5,000 HP by 97%" (67 characters, only the damage EXCEEDING the threshold is reduced), and `nullify_damage` = "Nullify Damage (Damage Negation, 100% Reduction)" demanded a "nullifies … damage" verb that does not exist — upstream equates nullification with a 100% reduction in its own notes (Jinbe #3774, Komurasaki #3217), so it matches the by-100% provider wording including the typed "from [X] enemies by 100%" form, while a 100% THRESHOLD cut stays Threshold DR (the corpus names Percent DR / Threshold DR / Damage Nullification as three distinct buffs); both subsets stay INSIDE `reduce_damage` because 39 of 57 threshold-only characters have no other reduction clause; `boost_atk` means "grants an ATK MULTIPLIER" and is NOT an umbrella (no sibling is a subset of it): it rejects the amplifier "increases boost effects of ATK Up buffs by Nx" (which grants no ATK and is `effect_boost`), and neither matcher gap may span another effect verb so a multiplier belonging to a later clause cannot be claimed — the verb guard deliberately excludes increases/sets/adds because those collide with buff NAMES ("Increase Damage Taken"), and with the guard doing that work the ATK→multiplier window is 160 rather than 80 so long enemy-state lists are no longer lost; flat "boosts base ATK … by 1,000" (`boost_base_atk`) and percent "boosts Final Tap ATK … by 30%" (`final_tap_atk_boost`) carry no "Nx" and are separated by the multiplier requirement alone; slot-change taxonomy — `change_slots` rejects the "becomes unable to change to [X] orbs" self-restriction (a limitation, not a change) and drops the never-used "transforms" alternative (upstream reserves "transforms" for character transformations); the new `change_slots_matching` = "Change Slots into Matching (Favorable Slot Change)" carries the wiki's type-adaptive family ("into (a) Matching orb(s)", 649 characters) as a strict subset of `change_slots` (umbrella kept, mirroring `change_block_slots`), with "into Badly Matching orbs" sabotage excluded; `swap_slots` = "Switch Orbs Between Slots (Slot Swap)" now matches OPTC-DB's real position-only wording "switches orbs between slots N times" (~73 units) instead of the "swaps" verb upstream reserves for unit/captain swaps and the Swap debuff (its 3 prior matches were all clause-bridge false positives); undirected "Randomizes all orbs" stays out of `change_slots` by design, matching OPTC-DB's own separate "Orb randomizers" filter family; `make_slots_favorable` = "Make Orbs Beneficial" — the OPTC-DB wording is "makes [X] orbs beneficial for \<scope\>" (Beneficial Orb Ability: a non-matching orb *counts as* beneficial, the orb itself is unchanged), which is a DIFFERENT wiki mechanic from Favorable Slot Change (actually changing an orb *into* a matching orb per character, currently covered by `change_slots`), so the key is labelled for what it detects; "favorable"/"advantageous" is never the verb upstream and that matcher alternative matched zero characters, and neither matcher gap may span another effect verb so "Makes PERFECTs harder to hit …, changes … orbs … into Matching orbs" cannot bridge into a false beneficial-orb tag; effect-target scope (captains/subs/crew/self) is DATA-GATED — the parser derives a scope for every cure/removal match from the qualifier trailing that clause ("on this character" => `self`, otherwise `crew`; never read from a neighbouring clause, so an ATK boost's class wording or an "If your Captain is …" condition cannot mislabel a cure), the catalog exposes `availableEffectTargetScopes` + a per-scope `effectTargetScopeMatchingCharacterIds` index whose turn buckets nest INSIDE each scope (a character can cure at one turn count on itself and another crew-wide), the picker offers exactly the populated scopes plus "any" instead of a static all-five list, and the filter narrows to the requested scope for non-captain requirements while captain structured effects keep resolving through `captainAbilityEffectMatches`; including `remove_sfx` = OPTC-DB "Blindness"; `remove_special_bind` = "Special Bind (Silence)" — "Silence" is the in-game label for the same specials-locking debuff OPTC-DB usually words "Special Bind", so both wordings fold into the one key via the `silence` TARGET_ALIASES alias (with the turn count preserved) and the duplicate turn-less `remove_silence` picker key is retired; it is NOT Despair, which is shown in-game as "Gloom"; potential-ability detection is name-alias keyed on the OPTC-DB `potentialAbilities[].Name` string, which is frequently NOT the app's display label — the 2026-07-22 potential audit added the real OPTC-DB Names to `POTENTIAL_ABILITY_ALIASES` so 10 previously dead 0-match keys resolve: `Last Tap` = Final Tap Sugo Special and `Rush` = Rush Sugo Special (both carried in `potentialAbilities` because their dedicated `finalTapData`/`rushSugoSpecialData` fields are null on every character, so those two keys now list `potentialAbilities` as a source); `Cooldown Reduction` = Own Special Charge Time Reduced; `Double`/`Triple Special Activation` = Special Double/Triple Launch; `Reduce Ship Bind duration`/`Ship Bind Resistance`/`Ship Bind` = Bind Ship Ability Resistance; `Reduce Special Use Limit duration`/`Special Use Limit` = Limit Special Uses Resistance; `Enrage/Reduce Increase Damage Taken duration`/`Enrage/Increase Damage Taken` = Provoked ATK Boost/Received Damage Up Resistance; `Nutrition/Reduce Hunger stacks`/`Nutrition/Hunger` = Recovery ATK Boost/Hunger Resistance; and the short newer `Slot Bind` Name folds into Slot Bind Resistance alongside `Reduce Slot Bind duration` — while `Slot Barrier` (a distinct stacks-based debuff) is deliberately kept OUT of `slot_changes_impossible_resistance`, and fear / rcv_bind / recoverable_hp_down / slot_changes_impossible stay no-data (absent from live optc-db `details.js`); special-cooldown semantics — `reduce_special_charge` matches only the canonical start-of-fight "Reduces Special Cooldown of \<scope\>" wording, while the distinct rewind-restore / advance-to-MAX charge family is `restore_advance_special_charge`, excluding ship special cooldown; the SHIP's own Special has its own cooldown (what Ship Bind disables), so `reduce_special_charge` now carries the same `(?!\s+of\s+ship)` guard as `restore_advance_special_charge` and `reduce_ship_special_charge` matches OPTC-DB's real "reduces Special Cooldown **of Ship**" wording instead of the never-occurring "ship special" adjacency that left it dead at 0 matches while its 16 characters were absorbed by the crew key; both are labelled with the OPTC-DB primary + community alias ("Reduce Special Cooldown (Reduce Special Charge Time)", "Reduce Ship Special Cooldown (Ship Special Charge)"); `reduce_damage` requires "reduces" to directly govern "damage received/taken", so glass-cannon downsides ("increases damage received"), counters ("deals Nx the damage taken"), heals ("recovers N% of damage taken"), and "Increase Damage Taken" debuff cures are not mis-tagged; `heal_hp` tolerates decimals in the gap so RCV-scaled heals like "recovers 1.5x character's RCV in HP" match, and accepts the legacy "health" wording; `special_damage` requires "deals" to govern an offensive "damage" — defensive "[BOMB]/[SUPERBOMB] orbs will deal N% less damage to the crew" and "reduces damage" are excluded via a negative lookbehind; `chain_multiplier_multiplicative_boost` requires the genuine "boosts (the) chain multiplier by Nx" wording, so growth-rate boosts ("Chain Multiplier Growth Rate by Nx" → `chain_multiplier_growth_rate`) and "ATK … at the start of the chain, by Nx" boosts are not mis-tagged; `chain_multiplier_growth_rate` in turn requires the genuine grant "(Boosts\|Increases) Chain Multiplier Growth Rate by Nx" (the by-Nx multiplier fingerprint), so growth-rate buff amplifiers that grant no growth rate themselves — "increases duration of any Chain Multiplier Growth Rate buffs" (Edward Newgate #4216) and "increases boost effects of … buffs" (Roger & Rayleigh & Gaban #4387) — plus the trigger condition "you gain a Chain Multiplier Growth Rate buff" (Dorry & Broggy #4436) are not mis-tagged as growth-rate grants; `boost_type_effects` matches a genuine Color Affinity / Type Effects GRANT ("boosts [the] Color Affinity of [scope]", possessive "their/its Color Affinity by Nx", or verb-anchored "boosts [Super] Type Effects"), not the old bare "color affinity" literal that mis-tagged every reference to the buff — effect_boost ("increases boost effects of Color Affinity buffs"), buff-duration extenders ("increases duration of any Color Affinity buffs"), conditions ("uses a special with / to boost a Color Affinity buff"), converts, and enhance-enablers; status-effect cures worded "reduces \<target\> duration completely" now strip the trailing "duration" keyword (like the "by N turns" form), so exact-match cures such as `remove_bind` ("reduces Bind duration completely" → RRG #4257, S-Shark #4311/#4312, Kizaru #4544) and `remove_damage_reduction` ("removes Percent Damage Reduction duration completely") are no longer missed; upstream's INVERTED cure grammar "Recovers \<N\> turns of \<target\> on self" (verb "recovers", count BEFORE the status, no "duration" keyword) is now a third cure pattern, recovering the only two Paralysis cures that no key tagged at all (Squard #642/#643 sailor ability, `remove_paralysis` 572 → 574), and the cure scope resolver accepts "on self" alongside the dominant "on this character" (499 uses vs 2) so those two publish as `self` rather than a crew-wide cure; both cure patterns also carry a symmetric anti-bridge guard, because each lazily scans to its terminator and would otherwise start at the FIRST verb in the sentence and swallow whole neighbouring clauses: "reduces \<target\> duration completely" now refuses to cross a `reduces`/`removes` (Kalifa #1295 "Reduces Paralysis duration by 3 turns, removes Poison duration completely" captured "Paralysis duration by 3 turns, removes Poison" and published a PERMANENT Paralysis clear for a 3-turn cure, while the real target Poison went unrecorded — 50 characters bridged, 61 records corrected across 13 keys, Chopper #2796 alone falsely claiming a permanent clear on 8 of them), and "reduces \<target\> duration by \<N\> turns" now refuses to cross a `completely` (Luffy #4129 "removes enemies' ATK Up and Enrage duration completely, and delays all enemies by 2 turns" reached forward past the finished clause into an unrelated turn count via a verb the guard does not list, publishing a bogus 2-turn removal beside the correct permanent one — 6 records, 0 replacements, and it also closes the previously-flagged case of a removal target swallowing an "increases duration of any \<X\> buffs" extender: Sabo #4291/#4292, Shinobu & Otama #4518); genuinely level-scaled specials keep BOTH records (Mansherry #1078 cures 3 turns at level 1 and clears permanently at max, since upstream lists every level in one string); `remove_bind` (522 -> 527) adds the last three cure readings the patterns could not read: a duration RANGE with a zero floor now publishes `minTurns: null` ("cures, but no guaranteed floor") instead of 0, because a range publishes its lower bound and returning 0 made the consumer's `minTurns <= 0` check discard the whole match TAG INCLUDED - Boa Hancock #4398/#4399 ("reduces Bind duration by 0-10 turns depending on the number of \<RCV\> orbs used in normal attacks") cured Bind but was absent from the key entirely, unfilterable even with no turn requirement, and now resolves through an unfiltered search while any "N+ turns" filter correctly skips her (the corpus's only zero-floor range; the other 17 all start at 1+, and no literal "by 0 turns" exists); and upstream's ELLIPSIS "reduces Bind and reduces enemies' Percent Damage Reduction duration by 3 turns" (Monet #2010/#2011), where the first target has no duration of its own and the trailing "by 3 turns" distributes across both, so the anti-bridge guard correctly refused to cross the second verb and dropped the cure - Fandom confirms the intent ("reduces Bind duration by 3 turns, and reduces all enemies damage reduction duration ... for 3 turns", filed under Category:Bind Reduction). The ellipsis is separated from the bridge by requiring the first target to carry NEITHER "duration" NOR "turns": Dogstorm #2168 "Reduces Special Bind duration by 4 turns and reduces enemies' Threshold Damage Reduction duration by 3 turns" must keep its 4-turn cure and never be re-read at 3. `remove_bind` is exact/endsWith rather than possessor-blind `includes`, so it excludes the three real sibling debuffs (Special Bind 763 uses, Slot Bind 20, Ship Bind 8); its fourth exclusion, "orb bind", matches nothing in the corpus and is vestigial; the same exact/endsWith rule also cost it a cure through the SHARED segment splitter: `isSlotScopedTarget` fires on a substring, so a target LIST that merely contained a slot-scoped entry took the whole-target-only path (which exists to preserve slot tokens the split path drops) and was never split - "Reduces Bind and Slot Bind duration by 5 turns" (Romy & Yorueka #4031) produced only the unsplit "bind and slot bind", which `remove_bind` matches on `endsWith(' bind')` and then vetoes through its own `!includes('slot bind')` exclusion, so a genuine 5-turn Bind cure resolved to Slot Bind alone; a slot-scoped target now emits its segments as well as the token-bearing whole (a lone slot-scoped target splits to itself, so this is a no-op there, and a lone "Slot Bind" still never leaks into the plain Bind key); `remove_despair` (476 -> 477) is possessor-blind `includes` rather than exact/endsWith, so a bridged target COULD still match it - but all 55 distinct targets that yield the key are clean status-name lists, confirming the anti-bridge guards hold; its one miss was an upstream VERB TYPO, "reducess Despair duration by 5 turns" (Makino & Woop Slap #3844, the corpus's only "reducess" - 1 of 7,183 verb instances - which `reduces?` cannot match), so all three cure patterns now accept `reduces{0,2}` (reduce/reduces/reducess) in BOTH the verb and the anti-bridge guard, since a guard that no longer recognises the typo as a verb could be bridged across it; the "Reduction" noun is still not a verb. The remove_despair/remove_sailor_despair boundary is correct in both directions - Sailor Despair is a distinct debuff disabling the SAILOR ability, and the `!includes('sailor despair')` veto is a substring check, but the segment split runs normally here (isSlotScopedTarget fires only on slot/orb variants) so "Despair and Sailor Despair" yields BOTH keys in either order, while a Sailor-Despair-only cure never claims the plain key (Basil Hawkins #3851/#3852 "reduces Bind and Sailor Despair duration by 6 turns" -> Bind + Sailor Despair, never Despair); its self scope is 15 characters at minTurns 99 with no finite bucket, which is CORRECT rather than a mis-scope - every self-scoped Despair cure really is a permanent clear ("reduces Bind, Despair, Special Bind and Paralysis duration completely on this character"); the same audit surfaced a CROSS-KEY severity defect in the max-level fold: a maxed character's special IS its final tier, but the fold deduped candidate records on key+source alone - a check that reasons only about MEMBERSHIP - so whenever a key already existed from tier 1 at a DIFFERENT turn count the max tier's record was silently discarded and the weaker LEVEL-1 count published (Gladius #1400 "reduces Bind and Despair duration by 1 turn ... by 2 turns" published 1; Machvise #1627, tiers 1/3/5, published 1). Dedupe is now on the full identity (key+source+minTurns+scope), so the max tier also publishes, while the INTERMEDIATE tier still stays out because the fold reads only the last tier: 144 records across 75 characters and 22 keys, every one at a turn count >= its own tier-1 count, with no key's membership changed; `special_damage_other` (464 -> 467) is the picker's TYPELESS damage key ("Other" = the type-matchup forced to x1, an axis-A property distinct from axis-B "True" damage, which ignores DEF but stays typed), and its matcher's gap was a bare `[^.]` that dies at the "." of a DECIMAL multiplier - so the whole "Deals 0.5x the damage dealt in the previous turn in Typeless damage" family was invisible while the integer form matched (Mihawk #717/#718/#1881, Eneru #3244/#3245/#4579, Doflamingo #3550, Ulti #3588, S-Hawk #4109, Sakazuki #4217); the gap now matches its own sibling `special_damage` at `(?:[^.]\|\.\d)`, which only ever crosses a period followed by a DIGIT and so still cannot run past a sentence boundary. The remaining 45 characters that mention "typeless" without matching are ONE correctly-excluded family - "adds \<N\>x character's ATK as Additional Typeless Damage for \<N\> turns", the post-attack additional-damage BUFF grant, a different mechanic owned by `additional_damage_boost` rather than damage dealt; the same ATD grant was also the key's only OVER-match: the 160-char gap reached out of one clause into a neighbouring "adds \<N\>x character's ATK as Additional Typeless Damage" grant, tagging 7 characters whose own damage is typed, percent, or not even theirs (Dogstorm #1570 "deals 60x character's ATK in \<STR\> damage to one enemy and adds 80x character's ATK as Additional Typeless Damage" is \<STR\>-TYPED so its matchup is not x1; Z #2329 deals percent damage; Akainu #4297/#4298 have no "deals" of their own at all - the bridged verb belongs to the ENEMY's Burn; also #1571, #2330, #2372), and without the `(?\<!\badditional\s)` guard the boundary was decided by PUNCTUATION rather than meaning, since identical grammar landed IN when the clauses were joined by "and" (#1570) and OUT when a period intervened (Koala #1241); a character with both a genuine direct typeless hit and an ATD grant correctly stays in (Sabo #1046). The picker entry is relabelled "Typeless Damage (Other)" per the METHODOLOGY dual-naming policy: the picker searches `[item.key, item.label]`, no label or key in the catalog contained "typeless", and so a search for the word upstream and the community both use returned ZERO hits across 467 characters - the mirror of the SFX/Blindness and Silence/Special Bind dead-key cases, where the label was too GENERIC to find rather than differently spelled; `boost_type_effects` (426) got the SAME dual-naming fix for the opposite reason - not too generic, but spelled the way OPTC-DB writes it ("Type Effects") rather than the way players and upstream more often say it ("Color Affinity", written 987x vs "Type Effects" 545x): the picker searches [key,label], nothing in the catalog contained "color affinity", so a search for the buff's most common name returned ZERO of 426. Relabelled "Boost Type Effects (Color Affinity)" (label-only, membership unchanged); the key itself is CLEAN on both sides - its verb-anchored grant matcher ("boosts ... Type Effects", "boosts (the) Color Affinity of", possessive "their\|its Color Affinity by Nx") has zero decimal-trap losses on the first alternative (the multiplier follows the anchor), zero bridges through the 120-char gap, and all 36 untagged Color-Affinity/Type-Effects mentions are correctly excluded non-grants (effect_boost meta "increases boost effects of", duration extenders, enhance-enablers, conditions, converts); a follow-up adversarial pass then caught the one over-match the manual audit missed - Vivi #4613 "If crew uses a Special to boost slot or type effects, further increases the effect by +0.3", an enhance-ENABLER condition where the bare infinitive "boost" bridged the old wide [^.]{0,120} gap to "type effects" (already correctly effect_boost); the "Type Effects" alternative now requires the verb to directly govern the noun (`boosts (the) (super) type effects`), dropping only #4613 (426 -> 425) and keeping all four genuine "boosts [Super] Type Effects" grants; `remove_damage_reduction` (348 -> 350) exposed a SHARED-normalization gap: normalizeTargetText stripped the "the"/"enemies'"/"buffs"/"crew" scope words but not the "all" QUANTIFIER, so "reduces all enemies' Percent Damage Reduction duration by 2 turns" (Bepo #4224/#4225) normalized to "all percent damage reduction" and failed the EXACT-match removal alias (target === 'percent damage reduction'); "all" is now stripped like "the", which - simulated corpus-wide - is +2 (Bepo) / -0 and touches no other key, because the includes/endsWith cure aliases already tolerated the prefix and only the pure-=== keys broke on it; flagged separately (NOT this key's duration mechanic): the "reduces enemies' Percent Damage Reduction EFFECT by <N>%" magnitude-reduction wording (Jesus Burgess #4101/#4102 "effect by 100%", Viola #4516 "effect by 80%", and the conditional "If the enemy has Percent Damage Reduction status, reduces ... effect" form on Lucci & Kaku #4227 / Newgate #4388 / Bonney #4467) is a different mechanic from a DURATION reduction and is covered by no key; `remove_threshold_damage_reduction` (338 -> 339) has the same one-off-typo shape: upstream's SOLE misspelling "Thredhold Damage Reduction" (Nefeltari Vivi #3667, 1 of 955 uses) was missed by the includes('threshold damage reduction') matcher, so it now also accepts 'thredhold damage reduction' - scoped to this key and unable to leak, since the exact-match remove_damage_reduction rejects the typo string anyway; the key is otherwise CLEAN (includes-based, so over-match-prone, but all 338 prior matches are genuine duration removals - the apparent "condition/potency" suspects are all long-list cures whose "duration by N turns" sits at the tail past a naive window, and #4293's clause is a captainAbilityVariant "Threshold Damage Reduction duration by 6 turns"); the adversarial pass then caught one over-match on this key that a naive condition/potency probe misses: Saintess Gunko #4612 "reduces all enemies' damage reduction (except Threshold Damage Reduction) duration by 15 turns" EXCLUDES threshold DR, yet the includes() matcher fired on the exclusion substring - so normalizeTargetText now strips an "(except <buff>)" parenthetical (the corpus's ONLY such clause), which both drops #4612 from `remove_threshold_damage_reduction` (338) and correctly reassigns her to `remove_damage_reduction` (351), the plain Damage Reduction she actually reduces; flagged for the SUPPORT audit: Shiryu #4600 is the same-shaped FP in `support_reduce_enemy_effect_turns_damage_threshold` via the identical "(except …)" clause in supportData, which the cure-pipeline strip does not touch (that support regex needs its own guard); `remove_atk_down` (336 -> 338) is a crew ATK-Down cure that was CLEAN on membership (zero FPs; the includes matcher's `!chain atk down` exclusion correctly drops the distinct "Minimum-/Maximum-Chain ATK Down" debuffs, which the segment-split still routes independently), but the adversarial pass found a grammar gap my probe missed: Bobbin #2118/#2119 "Reduces enemies' Threshold Damage Reduction, ... duration by 5 turns, crew's ATK DOWN duration by 5 turns and changes orbs" - a single leading "Reduces" governing a COMMA-CONTINUED second "<target> duration by N turns" clause with no repeated verb, which neither the verb-anchored pattern nor the "and reduces" ellipsis could read. A new shared TURN_PATTERN anchors on a completed "turns," (optional "and"), guards its body against verbs/duration/turns/comma, and captures exactly one self-contained continuation segment - corpus-wide the ONLY occurrence of the shape (+2 Bobbin, 0 collateral across every key); flagged (missing key, not a fix): "Minimum-/Maximum-Chain ATK Down" has no removal key (remove_chain_coefficient_reduction is a different debuff), so those cures are unfilterable (Ace #4067/#4068, S-Snake #4167, Robin & Saul #4594, etc.); `tap_timing_requirement` (331, verified-correct) is a broad PERFECT-anchored matcher and the broad rationale HOLDS: every non-orb-keep "PERFECT" in captain/special text is a genuine tap-timing gate - chains ("chain of Good > Great > PERFECT hits"), triggers ("each time you hit a PERFECT"), the "until the first hit other than PERFECT" hold, the "Barrier Penetration Enabled character hits a PERFECT" gate, and the tap-grade SCALING table (St. Shamrock #4611 "MISS 3x; GOOD 3.25x; GREAT 3.5x; PERFECT 3.75x; EXCELLENT 4x") are all in-scope; zero over-match. The 63 untagged tap-adjacent chars are correctly OUT - 54 are "Chain Tap Timing Bonus" (a DIFFERENT mechanic, unfilterable, flagged) and 8 are "changes orbs for 1 GREAT hit" (an orb-effect duration unit, not a gate). Its only defect was the DEAD LABEL: "Tap-Timing Requirement" returned 0 hits for "perfect" - the word players most use - so it is relabelled "Tap-Timing Requirement (PERFECT)" (label-only, membership unchanged); a post-merge adversarial pass then found a LARGE under-match the label work had masked: `\bPERFECT\b` cannot match the PLURAL "PERFECTs"/"PERFECTS" (the `\b` needs a non-word char after "PERFECT", but "s" is one), and that plural is the DOMINANT captain gate wording - so ~105 genuine gates were missed ("after scoring 3 PERFECTs in a row" Gear Third Luffy #217, "after N consecutive PERFECTs" Law #2001, "depending on how many PERFECTs scored" Akainu #2022, "If you score 2 PERFECTS" Morley #2568); four PRECISE plural-gate alternatives were added (PERFECTs in a row / consecutive PERFECTs / how many PERFECTs / hit\|score N PERFECTs) taking the key 331 -> 436 (+105, 0 losses), deliberately NOT the broad `\bPERFECTs?\b` which would also pull in the out-of-scope "makes PERFECTs easier/harder to hit" (tap-difficulty modifier) and "makes PERFECTs consume [RCV] orbs" (orb mechanic) - neither gates an effect on tap timing; `chain_multiplier_additive_boost` (2 -> 312) was the same decimal trap at its worst: a chain ADDITION is ALWAYS fractional ("Adds 0.5x to Chain multiplier for 2 turns" - Kizaru #977; "Adds 0.1x ..." #1066; "Adds 0.2x ..." Binz #1105), so the decimal sits INSIDE the gap between "adds" and "to" and the bare `[^.]` died there on essentially every genuine member - the key matched 2 of 312 and was effectively dead - and the only 2 survivors were the only two with an INTEGER amount. The matcher now anchors on ADJACENCY rather than spanning a gap (adds -> amount -> optional range/parenthetical -> optional "to (the)" -> optional "base" -> the literal "chain multiplier"), which also recovers the only "to"-less wording ("adds 0.8x Chain multiplier", Belo Betty #3406) and, by requiring the word "multiplier", provably stays out of the neighbouring chain mechanics (across all 319 real matches the matched "chain" is followed by "multiplier" every time). Its old second alternative `/\bchain\b[^.]{0,80}\b\+\d/i` is DELETED rather than widened: it was structurally dead and always had been, since `\b` before `+` requires a WORD character immediately before it while upstream always writes a space (" +0.2x") - 0 matches corpus-wide, and repairing it instead would inject 90 false positives drawn from five other chain concepts. Every one of the 310 recovered characters was checked against verbatim text rather than trusted from the delta (a gains-only measurement cannot see a false GAIN): all are genuine additions, including the range ("adds 1.5x-2.5x to Chain" - Nami #3789/#3790), parenthetical ("adds 2.0x, preventing buff clears, to Chain" - King #3897, S-Snake #4166), enhanceable ("Adds 1.8x, can be enhanced up to 2 times, to Chain" - Vegapunk #4136) and captain wordings, plus a 2-character "Base Chain multiplier" variant (Ace #3829/#3830); zero bridges into the neighbouring chain mechanics, which are separate keys (Chain Multiplier Limit, Chain Coefficient Reduction, Chain Multiplier Growth Rate); note `remove_paralysis` and its siblings are CURES only — the 29 characters that INFLICT Paralysis on enemies ("paralyzes (60%) all enemies for 3 turns", e.g. Nami) have no picker key and stay unfilterable; `tap_timing_requirement` matches any `PERFECT` tap-timing signal on captain/special boosts ("chain of … perfect hits", "after the Nth PERFECT in a row", "until the first hit other than PERFECT", "each time you hit a PERFECT"), not just the narrow "perfect hits" wording, while excluding the ubiquitous orb-keep form "hit a PERFECT with \<char\>, keep \<char\>'s orb" (a conditional orb-keep, not a boost requirement); `remove_despair` excludes "Sailor Despair" — the distinct debuff that disables a character's sailor ability (cured by "Reduces Sailor Despair duration … on this character" → `remove_sailor_despair`), not the Captain-ability Despair — so a Sailor-Despair-only cure is no longer mis-tagged as a Despair cure; status-effect duration reductions also tolerate the upstream "reduces \<status\> duration N turns" wording with the "by" dropped (e.g. Luffy & Whitebeard #3728 "reduces Paralysis and Despair duration 1 turn"), while reactive "reduce Paralysis duration by half" partial reductions stay schema-limited because a turn count cannot express "half"; `change_slots` detects genuine orb-type conversions ("changes [X] orbs into [Y] orbs") but excludes "changes the Orb Multiplier / Amplification / Effects" — those alter an orb's multiplier/effect, not its type, so they are not a slot/orb-type change; `defeat_enemy` matches ONLY the instant-KO / execute wording "instantly defeats [all/one] enemies [with HP ≤ N% their MAX HP / ≤ Nx character's ATK]" (including the "chance to instantly defeat each enemy" and conditional "instantly defeats them/all enemies otherwise" forms), an execute effect that appears only on specials (0 captain fields upstream), so the conditional kill-streak captain boost "If you defeat an enemy, increases ATK boost slightly" (a stacking ATK trigger — captainAbility 34→0) and the defensive guard "Protects from defeat …" (Loss Prevention, the opposite effect) are no longer mis-tagged as Defeat Enemy; `percent_damage` matches both the canonical "deals N% of enemies' current HP in [True] damage to all/one enem(y/ies)" wording and the newer verb-first form "reduces (one) enem(y's/ies') HP by N% [(ignoring all defensive effects)]" (e.g. Law - Finger-Controlled Boulder #4185, whose upstream text moved off the "deals … current HP …" form), scoped to enem(y's/ies') so it never catches the SELF HP-cost "Cutting special, reduces crew's current HP by N%" — this replaces a dead `/cuts … HP by N%/` alternative that matched no OPTC-DB crew text; `boost_slot_effects` (Boost Damage) keys ONLY on the literal "Orb Effects"/"Slot Effects" grant ("Boosts Orb Effects of [scope] by N.Nx", incl. legacy lowercase and possessive "characters' slot effects by Nx"), dropping a former bare "orbs?" alternative that produced no true positives and only over-matched distinct orb mechanics — orb drop-rate ("boosts chances of getting [X] orbs"), makes-beneficial + ATK, "Orb Amplification" conditionals, and the RCV-orb HEAL boost "boosts the amount healed by [RCV] orbs by Nx" (a heal boost, already carried by `boost_rcv`) — narrowing captainAbility 29→3 and total 802→752; `special_damage_other` ("Other" = TYPELESS damage, the type-matchup-forced-to-x1 axis) now also catches typeless hits that carry a True/Fixed modifier between the words — "Typeless True damage" (Kizaru, Whitebeard), "Typeless Fixed True damage" (Cat Viper), "Typeless True Fixed damage" — which the old adjacent-only "typeless damage" pattern missed (total 370→464, +94, all genuinely typeless); it stays anchored on "typeless" so pure typed "[STR] True damage" and "Fixed True damage" (a distinct ignore-defense axis, kept under generic `special_damage` / `deal_fixed_damage`) are NOT folded in; `remove_atk_down` (Reduce Status Effect Duration) excludes "Minimum-Chain ATK Down" and "Maximum-Chain ATK Down" — distinct chain-conditional debuffs OPTC-DB models as separate matchers ("Sets minimum / maximum Chain multiplier ATK reduction to Nx") vs plain ATK DOWN ("Reduces ATK of … characters") — so a chain-only cure no longer mis-tags as a plain ATK-Down cure (mirrors `remove_bind` excluding special/slot/orb/ship bind and `remove_despair` excluding sailor despair): captainAbility 24→19, total 337→330 (drops Ace #4067/#4068 and Burgess #4101/#4102 Minimum-Chain, Sanji & Reiju #4483 plus special-source #4288/#4388/#4507/#4600), while #4101/#4102 keep the plain key via their genuine plain-ATK-Down special/support cure; `apply_resistance_reduction` (the enemy Type/Class damage-resistance-down debuff "reduces enemies' <Type/Class> Resistance by -N%") also matches the alternate OPTC-DB verb form "applies -N% <Type/Class> Resistance to enemies" (e.g. Caesar & Monet #4126 "applies -10% [QCK] Resistance to all enemies"), which the "reduces"-only branch missed — crew-side resistance GAINS use "boosts … Resistance" and stay excluded; overall matchCount 83→84, captainAbility 21→21; `delayed_effect_launch` (an effect scheduled to a LATER turn — "activates \<Special\> in the following turn", "boosts … for 1 turn in the following turn", "After N turns, \<effect\>", "launches … after N turn: …") requires the "after N turns" token to be comma/colon-terminated, so a gradual per-turn ATK ramp's cap ("increases own ATK multiplier by Nx at the end of each turn until it reaches a maximum Nx after 20 turns." — Elizabello II #2423/#2424, active from turn 1 with nothing launching on turn 20) is no longer mis-tagged; the "following turn" branch stays broad (no false positives); captainAbility 21→19, overall matchCount 179→177; `inflict_poison` (a unit that APPLIES the Poison debuff to enemies — "poisons all enemies", "inflicts all enemies with Poison/Strong Poison/Toxic") excludes the immunity-piercing ENABLER "allows effects that inflict Poison to ignore Debuff Protection" (optc-db `ignoreImmunities` — applies no Poison itself; mirrors the remove_atk_down ATK-Down-enabler exclusion) by stripping that clause before matching, and bounds the previously-unbounded bridge so a poison CURE/condition ("removes Poison duration completely … reduces the defense of all enemies"; "against enemies inflicted with Poison") is no longer bridged to a later "enemies"; captainAbility 11→4 (now exactly Magellan #1698/#2159 and Reiju #3402/#3403), overall matchCount 77→64; the shared status-duration TURN_PATTERN now parses RANGE turn counts "reduces \<X\> duration by N-M turns" (records the first/min number as the guaranteed reduction; "by 0-10 turns" → min 0 stays dropped), and guards the target against bridging a first no-turn-count clause across a second "reduce(s)/remove(s)" verb into a later "by N turns" (so #3902 "reduce Paralysis duration by half and reduces Special Cooldown … by 1-99 turns" does not tag remove_paralysis, and the special-cooldown "by 1-99 turns" never leaks into a status key) — purely additive, +11 range-turn matches across 8 keys (remove_atk_down/bind/burn/chain_coefficient_reduction/despair/enemy_barrier/paralysis/ship_bind), 0 removals, 0 captain-count change; `boost_base_atk` (Boost Damage) requires the grant verb to abut its object ("boosts base ATK of [scope] by N", the canonical OPTC-DB flat Base ATK Boost grant), dropping a former loose "boosts? … base ATK" bridge (and a dead "adds? … base ATK" alternative) that mis-tagged forms merely NAMING the "Base ATK Boost" buff — the effect_boost amplifier "increases boost effects of Base ATK Boost buffs by +N", the extender "increases duration of any Base ATK Boost buffs", and "enables Base ATK Boost buffs to be enhanced" (every dropped char keeps effect_boost) — narrowing captainAbility 19→16 and total 174→160; `end_of_turn_additional_damage` (Other) now anchors on the canonical tail "damage to \<scope\> enem(y\|ies) at [the] end of [each\|every\|the] turn" (the damage OBJECT bound to the end-of-turn timing), replacing an `/end of (?:each )?turn … damage/` matcher that wrongly assumed the timing preceded "damage" — so it both MISSED the real "deals Nx ATK in [Type] damage to all enemies at the end of each turn" wording (Kaido, Magellan, Eneru, Big Mom, Franky, King, …) and OVER-MATCHED non-dealing forms where an unrelated end-of-turn heal bridged to a later "damage" (reduce-damage received, the enemy-buff removal "removes/reduces enemies' End of Turn Damage/Percent Cut" where the ENEMY hits your crew, "ignore damage", and start-of-stage damage) — widening captainAbility 19→127 and total 58→194; the supportData sibling `support_end_of_turn_additional_damage` gets the SAME tail-anchored fix — a support GRANT of end-of-turn damage ("deals N% of enemies' current HP in damage to all enemies at the end of the turn for N turns" → Tsuru, Mihawk, Denjiro, Raizo, Eneru, Momonosuke, Katakuri & Oven) is now detected while support text that only REFERENCES the enemy's "End of Turn Damage" buff ("when the enemy enables an End of Turn Damage buff, reduces Increase Damage Taken …" and "removes/reduces enemies' End of Turn Damage/Percent Cut duration", the latter carried by `support_reduce_enemy_effect_turns_end_of_turn_damage`) is dropped — support 3→7; `lock_slots` (Slot) now requires "locks" to DIRECTLY govern an orb/slot object via a bounded scope whitelist ("locks all/own/the/your Captain's/[Type] orbs/slots"), replacing an `/locks? … (orbs?\|slots?)/` 80-char bridge that mis-tagged two Chain families whose "orbs" came from a LATER clause — "locks the chain multiplier at Nx … boosts Orb Effects/randomizes all orbs" (object is the chain multiplier, not orbs — Rayleigh #1882/#1883/#3018, Sabo #2440/#2441, Boa Hancock #2682) and the "Chain Lock" buff referenced by name ("increases duration of any Chain Lock buffs … changes/boosts orbs", "Chain Lock and Orb Amplification" — Vivi&co #3943/#3944, Trafalgar Law #4185, Luffy VS Kaido #4210/#4211, Stussy #4228, Hack #4421) — total 269→256, captainAbility 12→12 unchanged; `additional_damage_boost` (Boost Damage) now keys ONLY on the "Additional Damage" post-attack buff GRANT "adds Nx character's ATK as Additional [Typeless] Damage for N turns", replacing a loose `/adds? … damage/` + bare `/additional damage/` pair that mis-tagged every REFERENCE to the buff by name — conditions ("if your crew has Additional Damage buff", "N turns or more of Additional Damage"), the duration extender "increases duration of any Additional Damage buffs" (Curly Dadan #4296), a buff-list reference (Jinbe #4202), and the replace-trigger "if a crew member uses a special with an Additional Damage buff, replaces those buffs" (Garp #4239/#4240) — total 87→74, captainAbility 10→8; `other_damage_boosts` (Boost Damage catch-all) now requires "boosts" to DIRECTLY govern "damage dealt" ("boosts [the] damage dealt to enemies inflicted with \<status\>"), replacing a loose `/boosts? … damage dealt/` (120-char bridge) that mis-tagged the scaling CONDITION "boosts ATK … depending on the amount of normal attack damage dealt before this special" (Gol D. Roger #3176/#3177, where "damage dealt" is the scaling input, not the boosted object); the dead `/damage boost/` literal (0 matches, latent FP risk vs "Additional Damage Boost") is dropped — total 9→7, captainAbility 7→7 unchanged; `boost_against_poisoned_enemies` (Boost Damage) now anchors on "boosts … against … Poisoned enemies" (plus the verbose "against enemies inflicted with Poison / Strong Poison / Toxic"), replacing a matcher that required a "damage" token between "boosts" and "Poisoned enemies" (`/boosts? … damage … poisoned enemies/`) — that only matched when "Increase Damage Taken" happened to precede the target in the boost-against list, so it MISSED ~46 genuine boost-against-Poison units with no "damage" token (e.g. "against Poisoned and Strongly Poisoned enemies" — Blazing General Zombie #827, Boa Sandersonia #1055, Magellan/Caesar/Reiju); every "poisoned enemies" token in the corpus is a boost-against target (0 non-boost uses) — total 7→53, captainAbility 6→22; the two sibling boost-against matchers get the SAME re-anchoring — `boost_against_delayed_enemies` now keys on "boosts … against … delayed enemies" (dropping the old "damage"-token requirement `/boosts? … damage … delayed enemies/`, which missed ~112 genuine units and — via the "against" anchor — correctly excludes the trigger condition "If there are delayed enemies …", Kaya #4180, Onion&Pepper&Carrot #4181): total 4→116, captainAbility 4→26; and `boost_against_def_reduced_enemies` is re-anchored on "boosts … against … enemies with reduced defense" — the old matcher targeted the phrase "\<DEF\|defense\> reduced enemies" which NEVER appears upstream (canonical wording is "enemies with reduced defense") AND required a "damage" token, so it matched 0; all 140 "enemies with reduced defense" tokens are boost-against targets (0 non-boost uses): total 0→140, captainAbility 0→25; `superSpecialText` now flows through the shared `TURN_PATTERNS` / `SELECTED_DEBUFF` / `EXPLICIT_BUILDER_ABILITIES` pipeline instead of early-returning after only the `territory` special matchers — so genuine super-special "reduces enemies' \<buff\> duration by N turns" / "removes \<status\> duration completely" / "Deals N x ATK in Fixed True damage" / "Poisons all enemies" clauses now derive duration-removal, fixed-damage and inflict-status keys from super text (previously 0 from that source); the line-1335 `specialText \|\| captainAbility` guard keeps the FULL special-ability matcher set from running on super text so effects the base special already carries are not double-tagged, and `extractPrimaryAbilityBranchText` still bounds parsing to the primary activation branch — purely additive across 20 keys (+70 matchCount, all `superSpecialText`-source, 0 removals, 0 captain-count change; e.g. remove_special_bind +7, remove_threshold_damage_reduction +7, deal_fixed_damage +8, remove_despair +6); multi-level specials now also fold their MAX-LEVEL (last) activation tier — `analyzeBuilderAbilityText` keeps the existing primary/base tier AND adds the net-new keys of the last same-fingerprint tier via `extractMaxLevelAbilityBranchText` (a maxed character's special is its final, strongest tier, so late-tier effects such as Zoro & Sanji #4061 reducing enemies' Threshold Damage Reduction duration, previously visible only in the base tier, are now detected). Intermediate tiers are never parsed, so a superseded mid-tier effect cannot be over-claimed; scoped to specialText via a `foldMaxLevelTier` flag whose inner call is `false` to bound recursion to one level — purely additive across 37 keys (+116 matchCount, all specialText-source, 0 removals, 0 captain-count change; e.g. boost_atk +10, change_block_slots +9, boost_base_atk +9, remove_threshold_damage_reduction +4); `deal_fixed_damage` (Damage) now allows the Fixed-damage modifiers (True = also ignores damage reduction, Typeless = type-neutral) to appear in any order between "Fixed" and "damage" — the old `deals? … fixed(?: true)? damage` required "damage" to immediately follow the optional "True", so the "Deals … Fixed True Typeless damage" ordering (Typeless wedged before "damage") was missed (#3785, #4038, #4039); re-anchored on `deals? … fixed(?:\\s+(?:true\|typeless)){0,2}\\s+damage` with a bounded [^.]{0,160} bridge (max observed deals→fixed gap \<70, ReDoS-safe), "Typeless Fixed True damage" already matched via adjacent "Fixed True damage", and defensive uses do not exist (every "Fixed damage" token is the offensive deal) — total 282→285, captainAbility 5→5 unchanged); `remove_beneficial_effect` (Other) now requires an OFFENSIVE removal of the enemy's beneficial effects and excludes the DEFENSIVE "Nullif(y\|ies) Remove Beneficial Effects …" self-protection captain ability (Gol D. Roger #3176/#3177, #3786, #4057/#4058), where "Remove Beneficial Effects" is a NAMED enemy attack the crew nullifies — the old `removes? … beneficial effects` matched the "Remove" inside that named buff and mis-tagged all 5; a negative lookbehind `(?<!nullif(?:y\|ies\|ied)\\s)` before the verb drops the false positives while keeping any genuine "removes enemies' beneficial effects" wording (none exist upstream yet, so the key correctly resolves to 0) — captainAbility 5→0, total 5→0, all false positives purged from the seed); `remove_enemy_barrier` (Reduce Enemy Effect Duration) — the structured matcher (target contains "barrier", excluding "slot barrier"/"orb barrier") is verified-correct: it catches every genuine "reduces enemies' Barrier duration by N" cure (incl. multi-item lists and the "Barrier Penetration Enabled … reduces enemies' Barrier duration" trigger action) and captain coverage is complete (5/5), but 2 stale specialText false positives (#4561/#4562) survived the seed from an older parser — their only "barrier" mention is "normal attacks ignore damage reducing Barriers and Buffs", i.e. Barrier PENETRATION (bypass, a distinct mechanic) which the current matcher correctly rejects; the stale tags are purged so the offline apply-manual union does not re-add them — total 122→120, captainAbility 5→5 unchanged); `chain_multiplier_lock` (Boost Damage) now requires "locks" to DIRECTLY govern "chain multiplier" (`locks?\s+(?:the\s+)?chain\s+multiplier`, the Chain Lock grant that fixes the chain multiplier at Nx), replacing a `locks? … chain` 120-char bridge that mis-tagged (a) "locks all orbs …" specials whose only later "chain" is an unrelated clause (Chain Coefficient Reduction removal, Chain Multiplier Limit, "after the Nth chain", boosts Chain Multiplier Growth Rate) — the lock is on ORBS (lock_slots), not the chain multiplier — and (b) every reference to the "Chain Lock" buff by name that is not a grant ("increases boost effects of Chain Lock buffs" → effect_boost, "if your crew has Chain Lock" condition, "increases duration of any Chain Lock buffs" extender, "enables Chain Lock … to be enhanced"); "locks the minimum/maximum chain multiplier" stays with the distinct chain_multiplier_lock_min_max; all 3 captainAbility matches (#4267/#4268/#4289) were such references, so total 175→146, captainAbility 3→0, the 29 stale false-positive tags purged from the seed); `ignore_normal_attack_only` (Ignore Normal Attack Only) — "Normal Attack Only" is an enemy defensive buff that limits the crew to normal attacks (nullifying special damage/effects); besides the adjacent "ignoring Normal Attack Only" phrasing, OPTC-DB also lists NAO among the defensive effects a special "bypass[es]" ("makes Damage Specials … bypass all defensive Buffs, Barriers, Defense and Normal Attack Only" — Shanks #3970/#3971), which the "ignoring"-only matcher missed, so the matcher now accepts ignore/bypass verbs with a bounded, ReDoS-safe `[^.]{0,80}` bridge kept within one sentence — the "if your crew has Normal Attack Only when the special is activated" CONDITION wording (no ignore/bypass verb before NAO in the sentence) stays excluded — total 32→34, captainAbility 3→3 unchanged (both additions are specials); `remove_damage_reduction` (Reduce Enemy Effect Duration) — fixes a possessive-apostrophe bug in the shared `normalizeTargetText` target-normalizer: the strip `/\benemies'?s?\b/` could not consume the trailing apostrophe (a bare `\b` after the optional `'` fails because apostrophe→space is non-word→non-word), so it matched only "enemies" and left a stray "'" glued to the first buff name; the leading `' percent damage reduction` segment then failed the exact-match matcher (`target === 'percent damage reduction'`), so enemy Percent-Damage-Reduction removals were detected ONLY when PDR was NOT the first buff in the list ("Threshold Damage Reduction, Percent Damage Reduction and Increased Defense" caught; "enemies' Percent Damage Reduction and Increased Defense" MISSED even though Increased Defense from the same clause was caught). Consuming the possessive apostrophe (`/\benemies(?:['’]s?)?(?![a-z])/`, ReDoS-safe not-a-letter lookahead) makes the removal exact-match-able. Purely additive superset — the only builder key affected is `remove_damage_reduction` (total 99→347, captainAbility 2→11), with zero drops and zero other builder keys changed; the shared normalizer fix also correctly recovers the two supportData siblings (`support_reduce_enemy_effect_turns_damage_reduction` +21, `…_tap_timing` +7). All additions carry a genuine "reduces/removes enemies' (Percent) Damage Reduction … duration by N turns/completely" signature; "Threshold Damage Reduction" as the first/sole buff stays with the distinct `remove_threshold_damage_reduction` key, and crew's-OWN Percent-Damage-Reduction scaling references ("proportional to the strength of crew's Percent Damage Reduction buff" — Jozu/Oden #4293) stay excluded (the crew-strip leaves a `'s` prefix and no "reduces … duration" pattern exists there); `apply_weakened` (Apply Status Effect) — "Weaken" is an enemy damage-increasing debuff (like ATK Down / Increase Damage Taken) that OPTC-DB applies as "inflicts (all) enemies with Weaken by X.Xx"; the old bare `/\bweakened\b/` required the "-ed" suffix the debuff NEVER uses, so it matched 0 genuine appliers and fired only on the unrelated transform-form name "otherwise transforms into Weakened" (#3895/#3896 — 2 false positives). Re-anchored on the applier verb `/\binflicts?\b[^.]{0,60}\bweaken\b(?!ed)/` (ReDoS-safe; max observed inflicts→Weaken gap is 18) with a not-a-letter lookahead dropping the "-ed" transform name — total 2→30, captainAbility 2→4 (the 2 FPs purged from the seed; 30 genuine "inflicts … Weaken" appliers added). Non-applier references stay excluded because they use different verbs: the boost-against condition "boosts ATK against enemies inflicted with Weaken" ("inflicted", past tense) and the immunity-pierce enabler "allows … Weaken … to ignore Debuff Protection". Two further appliers (#4290/#4477) carry "inflicts … Weaken" only in superSpecialText and stay undetected under the pre-existing super-source special-matcher limitation (territory-only on super) — a separate cross-cutting item); `apply_ally_status_effect` (Apply Status Effect (Ally), group Other) — applying a beneficial status to the crew; the old `applies … to/for … crew\|characters` bridge mostly matched FALSE POSITIVES where "characters" was an unrelated boost target: "applies Territory: X to the field … boosts ATK of \<scope\> characters" (Territory-to-field, own `territory` key — 14 hits) and "applies the following: Deals … damage to all enemies" (end-of-turn damage — #4081/#4082, the entire prior captainAbility count). Re-anchored on the two canonical crew-status wordings that have no dedicated key — `/\bapplies?\b[^.]{0,80}\b(?:immunity\|turn progress effect)\b/` (crew debuff-immunity buffs "applies \<Status\> Immunity for N turns" — Burn / ATK DOWN / Increase Damage Taken / Chain Coefficient Reduction / Blindness / Tap Limit Immunity — and "Applies a Turn Progress Effect for N turns"), ReDoS-safe (max applies→status gap 56). Total 20→15, captainAbility 2→3 (16 FPs purged from the seed, 11 genuine appliers added incl. the previously-missed #3006/#3007/#3213/#3858/#3859/#4235/#4535 Immunity and #3680/#3816/#3992/#3993 Turn Progress Effect); all 15 are genuine crew Immunity/Turn-Progress applications, 0 FP); `class_change` (Other) — in-battle Class Change reassigns a character's Class 1 / Class 2 ("changes Class 1/Class 2 of all non-\<X\> characters to \<Y\> class for N turns" and the self-reclass "Changes own Type and both Classes"). The old `changes? … class` used a 120-char bridge to ANY "class", so a "changes orbs … boosts Advantageous Class" clause (Advantageous Class is a damage boost, not a reclass) bridged into false positives — #4372 (specialText) and #4477 (captainAbility, the entire prior captain count). Require the changed object to be the class itself — `/\bchanges?\b[^.]{0,40}\b(?:class\s*[12]\b\|both classes\b)/` — which also fixes the plural-form miss "both Classes" (#3522/#3523). Total 10→10 (2 "Advantageous Class" FPs purged, 2 plural-form reclass added), captainAbility 1→0; all 10 detections are now genuine reclass wordings, 0 FP. (9 further genuine reclass specials carry the wording only in superSpecialText and stay undetected under the pre-existing territory-only super limitation for SPECIAL_ABILITY_MATCHERS — a separate cross-cutting item.); `chain_multiplier_lock_min_max` (Boost Damage) — a Chain Multiplier min/max lock ("Chain Boundary") sets a floor/ceiling on the chain multiplier; the canonical object is the "minimum/maximum chain multiplier". The old `chain … (min\|max)` 80-char bridge over-matched every clause with a "chain" word near an unrelated "MAX"/"min": "…Chain Coefficient Reduction … recovers 30% of crew's MAX HP" (#3293/#3776/#4429/#4430 — MAX from MAX HP) and "Chain Coefficient Reduction and Minimum-Chain ATK Down …" (#4067/#4068 — Minimum belongs to the separate Minimum-Chain ATK Down debuff), so ALL 6 detections (incl. the entire captainAbility count) were false positives. Re-anchored on the real locked object `/\b(?:minimum\|maximum)\s+chain\s+multiplier\b/`; the only "Chain Boundary" mention (#3742 "boost effects of Chain Lock and Chain Boundary buffs") is an effect_boost REFERENCE, not a grant, so no genuine grant exists yet and the key correctly resolves to 0 (matchCount 6→0, captainAbility 2→0) while staying ready for a real "locks the minimum/maximum chain multiplier at Nx" grant. Plain "locks the chain multiplier at Nx" stays with the distinct `chain_multiplier_lock` key.; `remove_chain_multiplier_limit` (Reduce Status Effect Duration) — "Chain Multiplier Limit" is the ENEMY debuff that caps the crew's chain multiplier, removed via "reduces/removes Chain Multiplier Limit duration by N turns". The structured removal matcher's `\|\| target.includes('chain lock')` alias conflated it with the FRIENDLY "Chain Lock" buff (which locks YOUR chain multiplier at a value — the opposite, carried by `chain_multiplier_lock`): every corpus "chain lock" removal-target actually comes from "increases duration of any Chain Lock/Limit/Boundary buffs" (a friendly-buff EXTENSION), so all 3 "chain lock"-only matches (#4000/#4128/#4289 — the entire prior captainAbility count) were false positives and ZERO genuine enemy removals use "Chain Lock" without "Chain Multiplier Limit". Dropped the alias to require the real debuff name — total 102→99, captainAbility 2→0; the genuine "Reduces Chain Multiplier Limit duration …" removals (incl. multi-item lists like #4493) are retained; `remove_enemy_increased_defense` also tolerates the upstream "Increase Defense" spelling (missing trailing "d", Charlotte Smoothie #3935) via `/increased?\s+defense/`, safe because TURN_PATTERNS has already pre-scoped the alias target to the reduced enemy-buff name so it cannot bridge to a crew self-buff "increases defense" or to DEF-DOWN "reduces DEF of enemies" — 268→269; `extend_turn_duration` (the crew buff-duration extender) now anchors its verb directly on "duration of" (`/(?:increases?\|extends?\|prolongs?)\s+(?:the\s+)?duration\s+of/`) instead of a loose `/increases?…duration/` 120-char bridge, so the reduce-debuff-duration CURE family whose cured debuff is *named* "Increase Damage Taken"/"Increase Defense" (verb "reduces") no longer leaks in as a spurious extender — 264→213, 51 pure false positives dropped; `apply_increase_damage_taken` gets the same treatment — it now requires an APPLY grant (`/(?:inflicts?\|afflicts?)…increase damage taken/` or `/increases?…all enemies' damage taken/`) instead of the name-keyed `/increases?…damage taken/` bridge, so the 49 cure clauses it double-counted with `remove_increase_damage_taken`, plus boost-against gates ("inflicted with"), the effect_boost amplifier, and Increase-Damage-Taken-Immunity, no longer read as applying the debuff — 214→99; `delayed_effect_launch` also accepts the rare twin deferral wording "in the next turn" (Doc Q #4105, Blackbeard #4146 — 182→184) and anchors both deferral branches on the leading "in the", which is output-identical today but keeps the loosest matcher in the special set off two non-launch uses of the same nouns: the tap-timing condition "if during the following turn you score N PERFECT hits" (a growing shape, all 6 on ids 3831+) and the Chain carry-over "carries over Nx of Chain Multiplier on this turn to the next turn" (#3829/#3830); `boost_base_atk` joins `DURATION_TURN_KEYS` so the picker exposes a turn filter it always had the data for — 100% of its flat grants carry "for N turn(s)" (buckets 1t×94, 2t×42, 3t×22, 5t×2; the 10 without one are permanent captain passives) and it was the odd one out beside its multiplier twin `boost_atk` and its support counterpart `support_base_atk_boost_damage`, both already turn-enabled; membership is unchanged at 169; `boost_against_def_reduced_enemies` additionally covers the 2024+ vocabulary shift where the exploited enemy state is written as a STATUS NOUN ("boosts damage dealt to enemies inflicted with … DEF Reduction, or Paralysis", "boosts ATK against enemies inflicted with Defense Down") rather than the canonical "enemies with reduced defense" — 143→149 — while keeping the governing "boosts … (against\|damage dealt to) … enemies (inflicted with\|affected by)" frame, which is what holds the apply-side `apply_def_reduction` ("reduces the defense of all enemies by N%") and the enemy's "Defense Reduction Debuff Protection" immunity out of the key.) | `src/app/pages/auto-team-builder/auto-team-builder.page.spec.ts`; `src/app/pages/auto-team-builder/auto-team-builder-result-snapshot.utils.spec.ts`; `src/app/pages/auto-team-builder/special-charge-timeline.utils.spec.ts`; `src/app/core/services/character-mode-effects.utils.spec.ts`; `src/app/core/services/boosted-character-ranking.spec.ts`; `src/app/pages/auto-team-builder/team-mode-effects.utils.spec.ts` (includes the build-input guard derived from the source - every form control whose change handler resets the build binds `controlsDisabled()` (click-bound buttons are not yet covered) - and a build whose inputs change mid-run being stopped and publishing nothing; the class rule - any class selection means "characters of these classes" via `requireAllSelectedClassesInTeam: false` (owner, 2026-09-11), said under the select and reported per unit in the final report - the failure message naming only rules no fallback relaxes (the class pool and a Captain boost range; the in-game identity rule no longer heads every failure), Guided auto build naming what it would have to relax, and a reason under a disabled Build button; the requirement-source pop-up reading the catalogue once per opening, paging its list and dropping superseded responses; and the character-tag filter cases for all three pickers: narrowing, load-more still gated, manual/exclude selections independent, empty selection sends `undefined` while an empty match list gates to nobody, clear on modal close, and `resetPage()` clearing all three); `src/app/core/services/auto-team-builder.engine.spec.ts`; `src/app/core/services/auto-team-builder.service.spec.ts`; `src/app/core/services/auto-team-builder.recommendation-benchmark.spec.ts`; `src/app/core/services/auto-team-builder-ability-match.utils.spec.ts`; `src/app/core/services/ability-requirement-draft.utils.spec.ts`; `npm run test:i18n-regression` covers critical guided, preset, and compare EN/EL copy; the first-visit intro card (device-local dismissal, hidden on a saved-team or enemy handoff), the hero readiness line, and the three compact-when-empty sections that never hide active rules; the quick-start shortcuts on a fresh page only (gone with the first build-input change, back after Reset; the guided shortcut scrolls to and focuses Build), a saved-team handoff landing on every type and class with Build ready, the select-only one-tap fix beside the disabled-Build reason, and the "all selected is no filter" rule line; "Copy debug report" under a result and under a failed build (`auto-team-builder-debug-report.utils.spec.ts`: schema, codes, team key, failure report, redaction, size budget) with its manual-copy fallback, failure codes and kept build timings, the "Edited locally" chip with its Final team report line, and the ability-catalog notice; close alternatives that name only the ranking dimension that decided, after any hard constraint, and a sub never summarised by its own Captain Ability (`auto-team-builder.engine.spec.ts`), the debug report's execution path (`auto-team-builder.service.spec.ts`), and the preset import's count of ignored Captain boost bounds | `e2e/regression-flows.spec.ts` covers guided controls across the browser matrix and keeps the full guided worker case visible as a documented browser-specific exception; `e2e/accessibility.spec.ts` covers guided toggle keyboard activation and compare-open state semantics; `e2e/smoke.spec.ts` covers route render; the recommendation benchmark spec covers representative utility, fallback, leader-scope, and explanation-trust cases | `npm run test:ci -- --include src/app/core/services/auto-team-builder.recommendation-benchmark.spec.ts`; `npm run perf:ability-filters`; `npm run perf:explanation-compare`; `npm run perf:memory-pressure`; `npm run perf:route-load`; `npm run perf:budget-report`; scoped `npm run test:e2e:*` browser runs; `npm run i18n:validate`; `npm run test:i18n-regression`; `docs/copy-review-checklist.md` for copy changes | `../optc-team-builder-brain/audits/869dwc7ww-recommendation-quality-benchmark.md`; `../optc-team-builder-brain/audits/869dvr7x6-ability-filter-performance.md`; `../optc-team-builder-brain/audits/869dvr7x5-explanation-compare-performance.md`; `docs/copy-review-checklist.md` | Auto builder engine, worker, requirements, route load, memory pressure, and recommendation trust | Strong |
| Auto Team Builder compare and explanation panels | `/tabs/auto-team-builder` compare mode and results | `src/app/pages/auto-team-builder/auto-team-builder-team-compare.utils.ts`; `src/app/pages/auto-team-builder/auto-team-builder-results-report-panel.component.scss`; `src/app/pages/auto-team-builder/auto-team-builder-results-comparison-panel.component.scss`; `scripts/perf-memory-pressure.mjs` | `src/app/pages/auto-team-builder/auto-team-builder-team-compare.utils.spec.ts`; Auto Team Builder page specs; `npm run test:i18n-regression` covers critical compare storage/import EN/EL copy | `e2e/regression-flows.spec.ts` covers saved/imported compare sources and readable diffs; `e2e/accessibility.spec.ts` covers saved/imported source changes, invalid import alerts, swap keyboard activation, live status roles, and focused axe scans; `perf:memory-pressure` covers large compare import restore and cleanup under constrained mobile conditions | `npm run perf:explanation-compare`; `npm run perf:memory-pressure`; `npm run perf:route-load`; `npm run perf:budget-report`; scoped `npm run test:e2e:chromium -- --grep "@accessibility"` when compare semantics or session-storage recovery change; `npm run i18n:validate`; `npm run test:i18n-regression`; `docs/copy-review-checklist.md` for copy changes | `../optc-team-builder-brain/audits/869dvr7x5-explanation-compare-performance.md`; `../optc-team-builder-brain/audits/869dwcbb9-storage-limits-recovery.md`; `../optc-team-builder-brain/audits/869dwcee1-memory-pressure-recovery.md`; `docs/copy-review-checklist.md` | Compare mode, result explanations, imported compare hydration, compare-entry load, compare-session storage recovery, and memory pressure | Strong |
| Captain Coverage and captain ability semantics | `/tabs/captain-coverage` | `src/app/pages/captain-coverage/captain-coverage.page.ts`; `src/app/core/services/captain-coverage.utils.ts`; `src/app/core/services/captain-coverage-filter.utils.ts`; `src/app/core/grammar/captain-boost-grammar.ts` (the Captain-boost grammar the dataset build reads too); `src/app/core/services/character-tag-set.utils.ts` (the character-tag index is built by the shared `buildCharacterTagMatchIndex` helper); `src/app/core/services/fixtures/captain-contract-cases.json`; `scripts/perf-route-load.mjs`; the page hosts the shared `app-ability-tag-set-picker` behind one trigger (`captain-coverage-ability-filter-trigger`) with the host opt-ins `modalScopeClass` / `collapsibleTileCounts` / `captainScopedTileCounts` / `showHelp` / `scopeMarkers`, and paints results one `RESULT_PAGE_SIZE` page at a time; the tier help and result cost hints are hand-rolled disclosures (not `ion-popover`) that close on a second press or on Escape, and the team draft parks `maxTotalCost` alongside the slots so a restored team keeps the cap that decides who fits a seat; the per-card HP/ATK readout is `card.leaderBoosts`, the Captain's and Friend Captain's own multipliers folded together MULTIPLICATIVELY by `combineLeaderCaptainCoverageBoosts` (two 1.5x leaders read 2.25x, matching the game) with a non-boosting leader folding in as the identity, while list membership stays Captain-driven; an EMPTY Friend Captain seat contributes x1 and is persisted as `null` by `buildSavedTeamSlots` rather than mirrored as the Captain's own id - the game does not require a Friend Captain, and mirroring made a saved team reload with every printed multiplier squared, because the mirrored seat is indistinguishable from the legal deliberate same-character choice whose boost DOES count twice, so an empty seat is recorded explicitly and never re-derived by comparing `slots[1]` to `slots[0]`; the result pass itself lives in `src/app/core/services/captain-coverage-result-pass.utils.ts` and runs inside `captain-coverage-filter.worker.ts` through `CaptainCoverageFilterRunnerService`, falling back to the SAME function in-thread when the browser has no Worker - which is what every unit test exercises, since the page spec builds the page class directly; the pass returns ids plus a boosted count rather than card view models, and only the painted slice is hydrated by `hydrateResultCards`, so a team write below slot 0 re-hydrates without re-filtering; every filter writer calls `runResultPass`, `ionViewWillEnter` calls it too because favorites are edited on other screens; `?captain=<id>` (from a character page's "See who this Captain boosts", 869f13c5c) starts a fresh team with that Captain before `?teamId=` is read, so a saved team arriving in the same URL wins, and an id that is not a Captain is dropped; a generation counter drops a reply a newer press has superseded, and the pass takes a per-evaluation `createCaptainBoostScopeCache()` so the captain's ability text is parsed once instead of twice per catalog character | `npm run test:captain-contracts` (including `scripts/lib/captain-grammar-parity.spec.ts`, the build and the app reading every shipped Captain's boosts and cost or rarity scope the same way); `src/app/pages/captain-coverage/captain-coverage.page.spec.ts`; `src/app/pages/captain-coverage/captain-coverage-captain-handoff.spec.ts`; `src/app/pages/captain-coverage/captain-coverage.resave-keeps-notes.spec.ts` (869f6td2b: a second Save keeps the notes and the ship that Saved Teams or Manual Team Builder added to the same team, driven against the real `UserStateService`); `src/app/core/services/captain-coverage.utils.spec.ts`; `src/app/core/services/captain-coverage-filter.utils.spec.ts` (Tier chips read tags from a list item's summary, and cost narrows a class-scoped tier); `src/app/core/services/captain-coverage.cost-rarity-scope.spec.ts` (a cost- or rarity-scoped Captain boosts exactly the characters inside its range, at both edges, and the result pass counts them - #458 Sengoku read 0 boosted of 2,009); `src/app/core/services/captain-team-condition-status.utils.spec.ts`; the empty state names the filters actually narrowing the list and offers the page's only clear-all (`clearAllFilters()`), asserted from the template; a Captain plus four subs with no Friend Captain reads as complete (`optionalSlotIndexes`) | `e2e/smoke.spec.ts` covers route render; `perf:route-load` covers Captain Coverage cold-start timing and route chunk size | Captain contract gate in `docs/maintainer-validation-guide.md`; `npm run perf:route-load`; broad `npm run test:ci` when runtime behavior changes | `../optc-team-builder-brain/CAPTAIN_ABILITY_COVERAGE_CLARIFICATION.md`; `../optc-team-builder-brain/CAPTAIN_ABILITY_TIER_COVERAGE_PLAN.md`; `../optc-team-builder-brain/audits/captain-coverage/` | Captain parser, generated tiers, runtime matching, result character-tag filtering, and route load | Covered |
| Player-facing release history (What's New) | "What's new" in the side menu footer, directly above Settings; opens a modal, not a route | `src/app/core/data/whats-new.data.ts` (one entry per shipped release, newest first, EN + EL); `src/app/shared/whats-new/whats-new-modal.component.ts`; the menu entry and `@defer` block in `src/app/layout/tabs.page.html`. A TypeScript constant rather than a fetched asset on purpose: every prefetched asset in this repo is unhashed, and a mid-deploy window where `ngsw.json` is the new build while an asset body is still the old one is what makes a service-worker install fail. The modal is deferred, so 121 releases of history never enter the initial bundle | `src/app/shared/whats-new/whats-new-modal.component.spec.ts` (ordering, both languages, quiet releases marked rather than dropped, menu position, defer); `scripts/check-whats-new.spec.ts` | No dedicated E2E. The modal is opened and scrolled during the live pass when a release ships | `npm run test:whats-new` (spec plus `npm run whats-new:check` against the real data); `npm run verify:local` runs it as the `whats-new` lane | `../optc-team-builder-brain/CLAUDE.md` (Post-Merge Release Suggestion); `.claude/skills/optc-release/SKILL.md` | Release entries exist for every version, read as player language, and never claim value a release did not have | Covered |
| Saved Teams, share links, export, import, and compatibility repair | `/tabs/saved-teams`; share import through `/tabs/manual-team-builder` | `src/app/core/data/published-teams.data.ts` + `src/app/pages/saved-teams/published-teams.utils.ts` (the **read-only, zero-backend half** of 869f12xc8: a curated set shipped in the bundle the way `whats-new.data.ts` ships release history. Nothing is submitted, so there is nothing to moderate, nothing stored on anyone's behalf, and no availability promise - the full shared layer stays the owner's decision and is not started. Every entry is a **worked example composed from the shipped dataset**, never a player's submission and never a claimed clear, and importing one carries that provenance into the saved team's notes because once it is in Saved Teams it looks like the reader's own. Guarded by `scripts/check-published-teams.mjs`, the `published-teams` lane: every character id exists, every stage is one of the 787, every entry is flagged a worked example, and **no two SUB slots share a `partyConflictKeys` entry** - a rule that applies to the four subs and never to the two leader seats, where the same character is legal twice); `src/app/pages/saved-teams/saved-teams.page.ts`; `src/app/pages/saved-teams/saved-teams-transfer.utils.ts`; `docs/saved-team-schema-lifecycle.md`; `scripts/fixtures/data/saved-teams-v1.json`; `scripts/fixtures/data/saved-team-share-v1-legacy-partial.json`; `scripts/public-entry-synthetics.mjs`; `scripts/perf-saved-team-codecs.mjs`; `scripts/perf-route-load.mjs`; `scripts/perf-memory-pressure.mjs` | `src/app/pages/saved-teams/saved-teams.page.spec.ts`; `src/app/pages/saved-teams/published-teams.utils.spec.ts`; `scripts/check-published-teams.spec.ts`; `src/app/pages/saved-teams/saved-teams-transfer.utils.spec.ts`; transfer fixture specs through `npm run test:ci`; `npm run test:public-entry-synthetics` covers the deployed-entry monitor contract; `npm run test:i18n-regression` covers critical import/share/storage EN/EL copy; a saved team with an empty Friend Captain seat reads as complete | `e2e/regression-flows.spec.ts` covers export, share code/link import, invalid payload rejection, and valid import; `e2e/accessibility.spec.ts` covers share-link/code keyboard activation, action feedback live regions, import modal focus/dialog naming, import error alerts, and manual-builder share hydration; `e2e/public-entry-visual.spec.ts` guards the share-link landing first viewport; `Public Entry Synthetics` checks a redacted deterministic `teamShare` landing on the deployed public site; `perf:saved-team-codecs` covers repeated large-payload encode/decode/validation cost; `perf:route-load` covers deterministic Saved Teams entry cold-start timing and route chunk size; `perf:memory-pressure` covers large Saved Teams import evidence under a constrained mobile profile | `npm run test:e2e:chromium`; `npm run test:public-entry-visual` for intentional share-link landing visual changes; `npm run perf:ability-filters` when ability filters are touched; `npm run perf:explanation-compare` for heavy import/share hydration; `npm run perf:saved-team-codecs` for transfer codec performance; `npm run perf:memory-pressure` for low-end large-import memory evidence; `npm run perf:route-load` for Saved Teams entry, Manual Team Builder share-link route-load, and route chunk budgets; scoped `npm run test:e2e:chromium -- --grep "@accessibility"` when share/import semantics change; `npm run i18n:validate`; `npm run test:i18n-regression`; `docs/copy-review-checklist.md` for copy changes; `npm run test:saved-team-codecs` when schema, parser, or storage recovery changes; `PUBLIC_ENTRY_BASE_URL=https://optcteambuilder.com npm run synthetic:public-entry` for deployed share-link landing evidence | `../optc-team-builder-brain/audits/869dbnqv3-saved-teams-import-export-share.md`; `../optc-team-builder-brain/audits/869dwc0wb-saved-team-compatibility.md`; `../optc-team-builder-brain/audits/869dwcbb9-storage-limits-recovery.md`; `../optc-team-builder-brain/audits/869dwcee1-memory-pressure-recovery.md`; `docs/data-schemas.md`; `docs/copy-review-checklist.md` | Saved team persistence, storage recovery, transfer contracts, codec performance, import/share hydration, route-load budgets, memory pressure, public share-link visual baselines, and public share-link landing synthetics | Strong |
| Saved Enemies and associated team links, both directions, and the declared enemy vocabulary | `/tabs/saved-enemies`; the reverse link renders on `/tabs/saved-teams` | `src/app/pages/saved-enemies/saved-enemies.page.ts`; `src/app/pages/saved-enemies/saved-enemies-transfer.utils.ts`; `src/app/pages/saved-enemies/saved-enemies-text-parser.utils.ts`; `src/app/pages/saved-enemies/saved-enemies-text-autocomplete.utils.ts`; `scripts/lib/enemy-mechanic-vocabulary.mjs` + `docs/enemy-mechanic-vocabulary.json` (869f1328q. The closed set of stage requirements, each with what it MEANS to a player and what the ENGINE does with it. **15 of the 38 mechanics carry `derivedAbilityKey: null`**, so ticking one derives no requirement, constrains nothing, and cannot appear in the Final team report - only the mechanic checklist can report it, as `unanswerable`); `src/app/pages/saved-teams/saved-team-enemy-links.utils.ts` (869f1327n. The enemy -> teams edge already existed as `SavedEnemy.associatedTeamIds`; the team -> enemy direction is **derived from it**, never stored. A second copy would need a saved-team schema entry, a migration rule and an export-payload slot, and would eventually disagree with the first) | `src/app/pages/saved-teams/saved-team-enemy-links.utils.spec.ts`; `src/app/pages/saved-enemies/saved-enemies.page.spec.ts`; `src/app/pages/saved-enemies/saved-enemies-transfer.utils.spec.ts`; `src/app/pages/saved-enemies/saved-enemies-text-parser.utils.spec.ts`; `src/app/pages/saved-enemies/saved-enemies-text-autocomplete.utils.spec.ts` | `e2e/smoke.spec.ts` covers route render | `npm run perf:ability-filters` when saved-enemy filters are touched | Backlog and QA audits in `../optc-team-builder-brain/audits/` | Saved enemy modeling and saved-team association | Covered |
| Character Boxes and box transfer | `/tabs/character-boxes` | `src/app/core/data/content-ladder.data.ts` + `src/app/pages/character-boxes/content-ladder.utils.ts` (a short, dated, hand-curated ladder of well-known content and what a box must hold to field a team for it. It says **field**, never **clear**: the dataset carries **787 stage names across 14 groups** and **no requirements at all** - zero rows mention player level, stamina or unlock - so difficulty is game-balance knowledge this project has no source for, and any milestone whose numbers are a starting point carries `provisional` through to the screen. **As of 2026-09-14 none is**: the four rarity floors that shipped provisional were confirmed by the owner, who is this project's only authority on difficulty, and each carries `confirmedOn` with that date. The guard also rejects the half-done flip - a confirmed entry whose note still says "provisional" - and an entry claiming both states at once (869f1t8wv). The shortfall reported is the LARGEST unmet axis, never their sum, because one character can satisfy several at once. Guarded by `scripts/check-content-ladder.mjs`, the `content-ladder` lane, which binds every milestone to a real `(group, stage)` pair so a stage renamed upstream turns a lane red instead of quietly becoming a lie - 869f12xb1); `src/app/pages/character-boxes/character-boxes.page.ts`; `src/app/pages/character-boxes/character-boxes-transfer.utils.ts`; a box can hand itself to Auto Team Builder as the candidate pool via a `characterBoxId` query parameter (869f12x47) - the builder already accepted a box as a pool restriction, so this is a link and a preset rather than a second builder; `src/app/shared/character-tag-filter/character-tag-filter.component.ts` | `src/app/pages/character-boxes/character-boxes.page.spec.ts`; `src/app/pages/character-boxes/content-ladder.utils.spec.ts`; `scripts/check-content-ladder.spec.ts` (covers the character-tag gate on both the paged query and the select-all-filtered sweep); `src/app/pages/character-boxes/character-boxes-transfer.utils.spec.ts`; the empty character list says its cause ("Already in box" on an empty box, "Favorites only" with none) and carries its own Clear filters | Not currently in `e2e/smoke.spec.ts`; use focused live checks for layout or import/export changes | `npm run test:ci`; `npm run build` | `docs/maintainer-validation-guide.md` | Box management and transfer format | Covered |
| Settings, local data transfer, inventory capture, and favorite ships | `/tabs/settings` | `src/app/pages/settings/storage-diagnostics.utils.ts` (what this device holds and how close to full, plus a counts-only diagnostics export safe to paste into an issue); `src/app/pages/settings/character-override-diff.utils.ts` (which characters are edited locally and what each edit changed, field by field, against the shipped dataset, newest edit first; per-character revert beside revert-all. The dataset nests stats as `stats.min.hp`/`stats.max.hp` while an override stores them flat, so reading `stats.minHp` reports every stat unchanged - 869f12x4e); `src/app/core/data/browser-storage-keys.data.ts` (every browser-storage key, classified durable / device-preference / transient / credential, with each durable one naming the export scope that carries it; since 869f63gug every durable key raises "Local changes pending" on the Drive card unless its record states a `pendingFlagExemption`); `src/app/pages/settings/settings.page.ts`; `src/app/pages/settings/all-data-transfer.utils.ts` (ten scopes, declared once in `ALL_DATA_TRANSFER_SCOPES`, asserted by `all-data-transfer.utils.spec.ts`; `crewForgeProfiles` was added by 869f12x4p after a full export was found to omit it); `src/app/pages/crew-forge/crew-forge-profiles-transfer.utils.ts`; `src/app/pages/settings/favorite-ships-transfer.utils.ts`; `src/app/pages/settings/inventory-capture.utils.ts`; `src/app/core/services/user-state.service.ts` (also owns **Game version** and **Hide units I can't get** - 869f13282, where the preference MARKS and the toggle is the only thing that removes, per the owner's mark-never-hide decision of 2026-09-15 - and `RECENT_CHARACTER_LIMIT`, the recently-viewed cap that 869f1327d named and justified rather than leaving as a bare literal) | `src/app/pages/settings/settings.page.spec.ts`; `src/app/pages/settings/character-override-diff.utils.spec.ts`; `src/app/pages/settings/all-data-transfer.utils.spec.ts`; `src/app/pages/settings/favorite-ships-transfer.utils.spec.ts`; `src/app/pages/settings/inventory-capture.utils.spec.ts`; `src/app/core/services/user-state.service.spec.ts`; `src/app/core/services/user-data-transfer.service.spec.ts`; `src/app/core/services/user-data-transfer.restore-safety.spec.ts` (869f63gpg: a restore parses every scope before it clears the device); `src/app/core/services/user-data-transfer.cold-session.spec.ts` (869f63gq1: a session that never opened Rumble or the Auto Team Builder still exports, counts and syncs the saved Rumble opponents and the boost list); `src/app/core/services/user-data-transfer.scope-register.spec.ts` (869f63gug: every durable key in the register is loaded, exported, counted, cleared by a restore and raises Local changes pending, run through the real services) | `e2e/smoke.spec.ts` covers settings route render; `e2e/accessibility.spec.ts` covers it under `@accessibility` - every `ion-button` on the screen carries an accessible name, and the About block a problem report quotes is populated rather than empty | `npm run test:ci`; `npm run test:saved-team-codecs` for saved-team/all-data payload recovery; `npm run build` | `docs/data-schemas.md`; `scripts/fixtures/data/README.md`; `../optc-team-builder-brain/audits/869dwcbb9-storage-limits-recovery.md` | Local storage, backup, import/export contracts, and storage-limit recovery | Strong |
| Account and Google Drive sync | `/tabs/account`; `/tabs/drive-sync` redirect | `src/app/pages/account/account.page.ts`; `src/app/core/services/google-account.service.ts`; `src/app/core/services/drive-backup.service.ts`; `src/app/core/services/drive-sync-state.service.ts`; `server/drive-sync-server.mjs` | `src/app/pages/account/account.page.spec.ts`; `src/app/core/services/google-account.service.spec.ts`; `src/app/core/services/drive-backup.service.spec.ts`; `src/app/pages/drive-sync/drive-sync-review.utils.spec.ts`; `src/app/pages/drive-sync/drive-sync-review.carried-merge.spec.ts` (869f63gq1: a reviewed Merge adds the opponents and Crew Forge profiles only Drive had, the device's copy winning a clash, and never merges two boost lists); `src/app/pages/drive-sync/drive-sync-review.order.spec.ts` (869f6td2j: a reviewed sync keeps each list in this device's order, then what only Drive has in Drive's order - Saved Teams stay newest-first); `server/drive-sync-server.spec.mjs` | `e2e/smoke.spec.ts` covers account route render; authenticated Drive flows require targeted live/manual checks | `npm run test:drive-sync-server`; `npm run build:pages` for web config strictness | `server/README.md`; `../optc-team-builder-brain/DRIVE_SYNC_PROFILE_NAVIGATION_CLARIFICATION.md` | Account, OAuth config, and Drive backup | Covered |
| Quest / Rumble engine divergences | Both builders - the record covers what each engine scores, consumes and reports | `src/app/core/data/team-builder-engine-divergences.data.ts`; `scripts/check-engine-divergences.mjs` | `scripts/check-engine-divergences.spec.ts` proves the record goes red when a cited symbol stops existing, when a deliberate difference has no decision, and when an unreviewed one carries one | No UI route; the record is read by maintainers before proposing the two engines be merged | `npm run engines:divergences` | `../optc-team-builder-brain/audits/2026-09-14-869f12x53-data-capture-wave-2.md` | Team building engines | Covered |
| Pirate Rumble character ranking, auto builder, and saved Rumble teams | `/tabs/rumble-characters`; `/tabs/auto-team-builder-rumble`; `/tabs/saved-rumble-teams` | `src/app/core/models/saved-rumble-opponent.models.ts` and `src/app/pages/auto-team-builder-rumble/saved-rumble-opponents-transfer.utils.ts` (opponent crews saved on their own and reloadable into a fresh build, added by 869f12x45 - a saved TEAM already carried its opponent, but only that team could use it); `src/app/pages/rumble-characters/rumble-characters.page.ts` (character-tag filtering is applied in-memory in `matchesFilters`, before the `visibleLimit()` slice, so `hasMore()` stays truthful); `src/app/pages/auto-team-builder-rumble/auto-team-builder-rumble.page.ts` (the manual slot picker hosts `app-character-tag-filter` and re-tests each candidate in-memory with `matchesCharacterTagSets` inside `manualPickerResults`, before the `.slice(0, 80)` cap, so the visible grid is the first 80 of the *narrowed* set; the selection is cleared in `closeManualCharacterPicker()`, the single funnel for every close path, so it cannot leak into a later open for a different slot); `src/app/core/services/auto-team-builder-rumble.engine.ts`; `src/app/core/services/auto-team-builder-rumble.service.ts`; `src/app/pages/saved-rumble-teams/saved-rumble-teams.page.ts` | `src/app/pages/rumble-characters/rumble-characters.page.spec.ts`; `src/app/pages/rumble-characters/rumble-characters-ranking.utils.spec.ts`; `src/app/pages/auto-team-builder-rumble/auto-team-builder-rumble.page.spec.ts` (includes filter-before-slice, the 80 cap still applied after narrowing, AND-across-groups/OR-within-group, empty selection inert, and clear-on-close/reopen); `src/app/core/services/auto-team-builder-rumble.service.spec.ts`; `src/app/core/services/auto-team-builder-rumble-focus.utils.spec.ts`; `src/app/pages/saved-rumble-teams/saved-rumble-teams.page.spec.ts`; the opponent-awareness line asking for an opponent instead of promising to counter 0 slots | `e2e/smoke.spec.ts` covers Rumble characters and auto Rumble builder routes; saved Rumble teams is unit-covered but not in smoke | `npm run test:ci`; `npm run build` | Rumble optimization notes in `../optc-team-builder-brain` when tasks target this area | Rumble ranking and team optimization | Covered |
| Crew Forge screenshot import and profile matching | `/tabs/crew-forge` | `src/app/pages/crew-forge/crew-forge.page.ts`; `src/app/core/services/crew-forge-image-import.service.ts`; `src/app/core/services/crew-forge-fingerprint.utils.ts` (the two numbers the matcher decides on, extracted so they can be pinned); `src/app/core/data/crew-forge-confidence.data.ts` (what the confidence score means, its measured landmarks, and the two bands the matcher actually produces); the import preview doubles as the review queue - cards ordered least-certain-first with a per-slot confirm, so a reader can see which of forty matches deserve a look; the same head line reports how many slots the profile's `matchThreshold` turned down, counting only `ambiguous` and never `empty`, so the invisible per-player dial is named as the cause rather than left to be guessed (869f13gay); the matching pipeline itself is written out in `docs/crew-forge-matching.md`; `src/app/core/data/crew-forge-built-in-profiles.ts` | `src/app/pages/crew-forge/crew-forge.page.spec.ts`; `src/app/core/services/crew-forge-image-import.service.spec.ts` | `e2e/smoke.spec.ts` covers route render; profile authoring needs screenshot-based live evidence | `npm run test:ci`; live artifacts under `../optc-team-builder-brain/live-artifacts/<task-id>/` for profile tasks | Crew Forge profile audits in `../optc-team-builder-brain/audits/` when created | Crew Forge OCR/profile matching | Covered |
| Data import, manual characters, and generated dataset integrity | Scripts and generated data | `scripts/import-optc-data.mjs`; `scripts/apply-manual-characters.mjs`; `scripts/upsert-manual-character.mjs`; `scripts/lib/dataset-integrity.mjs`; `scripts/lib/manual-character-overlay.mjs`; `scripts/lib/optc-dataset.mjs`; `src/app/core/grammar/captain-boost-grammar.ts` (the one Captain-boost grammar - the Captain boost columns and every tier's cost and rarity scope are read by it, and the app imports the same file) | `scripts/import-optc-data.spec.ts` (includes name normalization: an in-game glyph tag becomes its text); `scripts/upsert-manual-character.spec.ts`; `scripts/lib/dataset-integrity.spec.ts`; `scripts/lib/optc-dataset.spec.ts` (an import that changes only the timestamp leaves the five generated files byte-identical); `scripts/lib/manual-character-overlay.spec.ts`; `scripts/lib/manual-character-apply.spec.ts`; `scripts/lib/manual-character-prune.spec.ts`; `scripts/lib/captain-grammar-parity.spec.ts` (over the whole committed seed: every shipped Captain boost column rebuilds from the build path, and the Local edit path reads the same number) | No browser E2E by default; generated data changes are validated by script specs and targeted app specs | `npm run test:captain-contracts`; manual-character integrity row in `docs/maintainer-validation-guide.md` | `../optc-team-builder-brain/audits/869dwc0wh-manual-character-integrity.md`; character-specific audits in `../optc-team-builder-brain/audits/` | Dataset import and manual override ownership | Covered |
| OPTC DB release detector, source-contract canary, duplicate-dispatch idempotency guard, release-trigger notifications, detector status/history artifacts, release provenance, and post-dispatch production smoke | Scheduled/manual Actions and detector script | `scripts/check-optc-release-needed.mjs`; `scripts/backtest-optc-release-detector.mjs`; `scripts/release-detector-status.mjs`; `scripts/release-decision-history.mjs`; `scripts/release-provenance-report.mjs`; `scripts/post-dispatch-production-smoke.mjs`; `scripts/lib/release-trigger-policy.mjs`; `scripts/lib/release-trigger-notifications.mjs`; `.github/workflows/check-optc-db-release.yml`; `.github/workflows/release-android.yml` | `scripts/check-optc-release-needed.spec.ts`; `scripts/backtest-optc-release-detector.spec.ts`; `scripts/release-detector-status.spec.ts`; `scripts/release-decision-history.spec.ts`; `scripts/release-provenance-report.spec.ts`; `scripts/post-dispatch-production-smoke.spec.ts`; fixture replay under `scripts/fixtures/release-check/`; provenance fixtures under `scripts/fixtures/release-provenance/`; historical corpus under `scripts/fixtures/release-check/history/corpus.json` | No app browser path for detector decisions; workflow artifacts provide operational evidence, including source-contract failure details, duplicate-dispatch blocked state, compact `release-detector-status`, 45-run `release-decision-history`, post-release `release-provenance`, and post-dispatch production smoke JSON/Markdown artifacts | `npm run test:release-check`; `npm run data:backtest-release -- --json`; `npm run data:check-release -- --fixture=... --json`; `npm run release:decision-history -- --current-status ... --history-dir ...`; `npm run release:provenance -- --github-release ...`; `npm run release:post-dispatch-smoke -- --release-provenance ... --public-entry-report ...` for captured release evidence | `../optc-team-builder-brain/OPTC_DB_AUTO_RELEASE_RUNBOOK.md`; release-trigger, provenance, and post-dispatch smoke audits in `../optc-team-builder-brain/audits/` | Release detector, dispatch idempotency, notification, source-contract, decision-history, provenance, and post-dispatch smoke policy | Covered |
| Release readiness reporting | Maintainer release evidence and sign-off summaries | `scripts/release-readiness-report.mjs`; `scripts/fixtures/release-readiness/` | `scripts/release-readiness-report.spec.ts` | No UI route; output markdown and JSON are reviewed as release artifacts | `npm run test:release-readiness`; `npm run release:readiness -- --source ...` | `../optc-team-builder-brain/audits/869dwc0wc-release-readiness-summary.md` | Release readiness report contract | Covered |
| Post-merge smoke pack | Release-critical guide/help, guided/compare/share, and release-check handoff paths | `docs/post-merge-smoke-pack.md`; `package.json`; `e2e/smoke.spec.ts`; `e2e/regression-flows.spec.ts` | `scripts/post-dispatch-production-smoke.spec.ts` and `scripts/summarize-playwright-failures.spec.ts` cover the smoke decision and the runner's failure reporting; docs command verification covers the safe command blocks | `@post-merge-smoke` Chromium subset covers public guides, guided next-slot state, compare saved/imported sources, and Saved Teams export/share/import | `npm run test:post-merge-smoke`; `npm run docs:commands -- --brain-root ../optc-team-builder-brain` | `docs/post-merge-smoke-pack.md`; PR and ClickUp evidence for each run | Post-merge web and release-adjacent confidence ownership | Covered |
| Dependency maintenance and tooling updates | Dependabot, package/toolchain updates, workflow updates, GitHub Actions pinning, workflow timeout/concurrency budgets, browser-test tooling, release-check tooling, performance harnesses, release-readiness tooling, and native build tooling | `.github/dependabot.yml`; `package.json`; `package-lock.json`; `.github/workflows/`; `scripts/check-github-actions-pins.mjs`; `scripts/check-github-workflow-budgets.mjs`; `playwright.config.ts`; `e2e/`; `scripts/run-playwright-e2e.mjs`; `scripts/perf-*.mjs`; `scripts/check-optc-release-needed.mjs`; `scripts/release-readiness-report.mjs`; `android/` | `npm run doctor:maintainer -- --profile=ci --brain-root ../optc-team-builder-brain`; `npm run actions:pins`; `npm run test:actions-pins`; `npm run actions:workflow-budgets`; `npm run test:workflow-budgets`; focused script specs from the changed surface | No default UI path; browser, PWA, performance, or native release checks are selected only when the updated dependency or tooling can affect those paths | `docs/dependency-maintenance-policy.md`; `npm run verify:local` on the merged state (869f135u5 - this cell used to name PR `Test` and `Docs Integrity` workflows, which the no-CI-on-pull-requests policy removed, so it described a gate that had stopped existing) | `../optc-team-builder-brain/audits/869dwcbb3-dependency-maintenance.md`; `../optc-team-builder-brain/audits/869dwchu0-workflow-concurrency-timeouts.md` | Dependency cadence, update review, action-pin control, workflow-budget control, and rollback policy | Covered |
| Performance budgets and trend reporting | Scheduled/manual `Performance Budgets` workflow plus report-only local stress harnesses | `scripts/perf-ability-filters.mjs`; `scripts/perf-explanation-compare.mjs`; `scripts/perf-saved-team-codecs.mjs`; `scripts/perf-memory-pressure.mjs`; `scripts/perf-route-load.mjs`; `scripts/perf-budget-report.mjs`; `scripts/perf-budget-history.mjs`; `.github/workflows/performance-budgets.yml` | `scripts/perf-budget-report.spec.ts`; `scripts/perf-budget-history.spec.ts` | Browser and Node harnesses collect screenshots, timing JSON, codec timing JSON, memory-pressure JSON, route-load JSON, and production bundle stats for public entry, compare, Characters search, Saved Teams, and Captain Coverage routes | `npm run perf:ability-filters`; `npm run perf:explanation-compare`; `npm run perf:saved-team-codecs`; `npm run perf:memory-pressure`; `npm run perf:route-load`; `npm run perf:budget-report`; `npm run perf:budget-history` | `../optc-team-builder-brain/audits/869dwc0hh-performance-budget-tracking.md`; `../optc-team-builder-brain/audits/869dwc3z5-scheduled-perf-reporting.md`; `../optc-team-builder-brain/audits/869dwcee1-memory-pressure-recovery.md` | Browser performance, codec performance, route-load and bundle budgets, import/share hydration, memory pressure, and trend ownership | Covered |
| Docs integrity, command execution, drift detection, release runbook drift, and cross-repo reference hygiene | Markdown docs, audits, runbooks, command examples, repo file references, release runbook contracts, and feature-to-doc mappings | `scripts/check-docs-integrity.mjs`; `scripts/check-docs-commands.mjs`; `scripts/check-docs-drift.mjs`; `scripts/check-release-runbook-drift.mjs`; `docs/docs-drift-map.json`; `.github/workflows/docs-integrity.yml` | `scripts/check-docs-integrity.spec.ts`; `scripts/check-docs-commands.spec.ts`; `scripts/check-docs-drift.spec.ts`; `scripts/check-release-runbook-drift.spec.ts` | No UI path; validates checked-in Markdown links, anchors, repo refs, ClickUp URL shape, OPTC public URLs, allowlisted maintainer commands, release runbook workflow/package/policy alignment, and mapped feature changes without docs updates | `npm run docs:integrity -- --brain-root ../optc-team-builder-brain`; `npm run docs:commands -- --brain-root ../optc-team-builder-brain`; `npm run docs:release-runbook-drift -- --brain-root ../optc-team-builder-brain`; `npm run docs:drift -- --base-ref origin/main --head-ref HEAD --brain-root ../optc-team-builder-brain`; `npm run test:docs-integrity`; `npm run test:docs-commands`; `npm run test:release-runbook-drift`; `npm run test:docs-drift`; `git diff --check` | `docs/maintainer-operations.md`; `../optc-team-builder-brain/audits/869dwc3zd-docs-integrity.md`; `../optc-team-builder-brain/audits/869dwc3z7-docs-command-verification.md` | Docs/reference, drift, and maintainer-command hygiene | Covered |
| PR traceability | Human-authored PR template and workflow gate | `.github/pull_request_template.md`; `.github/scripts/check-pr-traceability.mjs`; `.github/workflows/pr-traceability.yml` | Workflow script coverage is the manual `npm run pr:traceability -- --pr <n>` run | No automatic PR check; run the gate manually or dispatch the `PR Traceability` workflow with a `pr_number` | `npm run pr:traceability -- --pr <n>`, run by hand before review or merge - there is no automatic check, which is why this row is Partial rather than Covered | `../optc-team-builder-brain/audits/869dwc3zc-pr-traceability.md`; `docs/maintainer-validation-guide.md` | PR evidence, verification, and ClickUp routing | Covered |
| Branch lifecycle and post-merge cleanup | Merged PR branches, repository rulesets, and maintainer cleanup decisions | `scripts/branch-cleanup-report.mjs`; `docs/branch-lifecycle-policy.md` | `scripts/branch-cleanup-report.spec.ts` | No UI path; report-only CLI reads GitHub/Git state and never deletes branches | `npm run test:branch-cleanup`; `npm run branch:cleanup-report -- --repo JohnChourp/optc-team-builder --format markdown`; docs integrity and drift checks for policy changes | `docs/branch-lifecycle-policy.md`; `docs/maintainer-operations.md`; `../optc-team-builder-brain/audits/869dwcbba-branch-cleanup-policy.md` | Branch cleanup policy and GH013 blocker triage | Covered |
<!-- docs-integrity-ignore-next-line: paired brain PR #90 adds the 869dwcedz audit before app PR closeout completes -->
| Player-facing FAQ | `/faq`, and `/tabs/faq` inside the shell | `src/app/pages/faq/faq.page.ts`; `src/app/pages/faq/faq.data.ts` (structure only - every word lives in the `faq` translation scope, with each entry declaring its bullets, links, and the app labels it quotes); `public/i18n/faq/en.json`; `public/i18n/faq/el.json` | `src/app/pages/faq/faq.data.spec.ts` walks the declared list and proves every bullet, link, and quoted label resolves non-empty in BOTH locales; `src/app/pages/faq/faq.page.spec.ts` covers rendering and accordion behaviour; `src/app/pages/public-primary-action.spec.ts` holds the page to one solid link, the first answer's Auto Team Builder (869f13c5p) | `e2e/accessibility.spec.ts` covers it under `@accessibility`: every question reachable by Tab, Enter expands one, `aria-expanded` updates where a screen reader reads it, answer links are named, and the heading outline is one `h1` plus a non-empty `h2` per section | `npm run test:ci -- --include src/app/pages/faq/faq.data.spec.ts --include src/app/pages/faq/faq.page.spec.ts --include src/app/pages/public-primary-action.spec.ts`; `npm run i18n:validate`; `npm run routes:sitemap-coverage` (the route is in the generated sitemap); the [copy review checklist](copy-review-checklist.md) | `docs/copy-review-checklist.md`; `../optc-team-builder-brain/audits/2026-09-14-869f12x4h-fixes-wave-2.md` | Player-facing help copy and its bilingual parity | Strong |
| What this app supports | `/tabs/supported`, reached from the side menu above the FAQ (private route - no `data.seo`, so it is excluded from the sitemap with a written reason) | `src/app/pages/supported/supported.page.ts`; `src/app/pages/supported/supported.data.ts` (structure and the verifiable claims only - every word lives in the `supported` translation scope); `public/i18n/supported/en.json`; `public/i18n/supported/el.json`; `scripts/check-support-claims.mjs` | `src/app/pages/supported/supported.data.spec.ts` proves every declared key resolves non-empty in BOTH locales and that the two key sets are identical; `scripts/check-support-claims.spec.ts` covers each failure mode of the claims guard | No dedicated E2E. The screen is static copy; what can silently go wrong is a CLAIM drifting from the configuration, which is a check rather than a browser assertion | `npm run test:support-claims`; `npm run i18n:validate`; `npm run routes:sitemap-coverage`; the [copy review checklist](copy-review-checklist.md) | `docs/ios-platform-footprint.md` (why there is no App Store build); `../optc-team-builder-brain/audits/2026-09-15-869f17h18-improvements-wave-10.md` | The player-facing support statement, its bilingual parity, and the configuration its claims are checked against | Covered |
| Landing page and the privacy, cookie, and terms pages | `/tabs/` landing; `/privacy`, `/cookies`, `/terms` and their `/tabs/` twins. The privacy and cookie pages name every measurement vendor the site loads - Google Analytics, Google Tag Manager, Microsoft Clarity and Cloudflare Web Analytics - in both languages, enforced by `VENDOR_DISCLOSURE` in `scripts/check-csp-policy.mjs` (869f135w2). Since 869f13c5m nothing but Cloudflare's cookieless edge beacon loads before a reader accepts analytics: the tag manager and GA4 load from `GoogleAnalyticsService.enable()`, the banner asks only where analytics can run and after the first in-app navigation, with Accept and Reject weighted equally (`shouldAskForAnalyticsConsent`), and the Cookie page and Settings say analytics is unavailable instead of offering a choice where it cannot run - all held by `src/app/core/services/analytics-consent-before-load.spec.ts` | `src/app/pages/home/home.page.ts`; `src/app/pages/privacy-policy/privacy-policy.page.ts`; `src/app/pages/cookie-policy/cookie-policy.page.ts`; `src/app/pages/terms-of-service/terms-of-service.page.ts`; `public/i18n/privacy-policy/`; `public/i18n/cookie-policy/`; `public/i18n/terms-of-service/` | One page spec each (`home.page.spec.ts`, `privacy-policy.page.spec.ts`, `cookie-policy.page.spec.ts`, `terms-of-service.page.spec.ts`) | `e2e/public-entry-visual.spec.ts` captures the public landing, and `e2e/smoke.spec.ts` visits `/tabs/privacy`, `/tabs/cookies` and `/tabs/terms` and asserts privacy's canonical; none of the three has a spec of its own beyond that | `npm run i18n:validate`; `npm run routes:sitemap-coverage`; `npm run seo:audit` (all four are generated public routes); `npm run test:security-config`, which fails if an injected origin stops being named in the privacy copy | `../optc-team-builder-brain/audits/2026-09-14-869f12x4h-fixes-wave-2.md` | Public landing and policy copy | Strong |
| SEO pages, guide discoverability, and GitHub Pages deploy | Public SEO content pages, guide/help routes, generated sitemap, public guide landing, Pages artifact, and post-deploy PWA cache freshness | `src/app/core/data/public-routes.data.ts` (the public route list, written once - it used to be written six times and `/faq` reached only one of them); `scripts/lib/public-routes.mjs` (how the plain-`.mjs` scripts read that TypeScript registry); `src/app/pages/seo-content/seo-content.data.ts` (the words of the four tool pages and three guides, written once - the page renders them and the generator builds their static fallback from them, where there used to be two different bodies sharing no sentence); `scripts/lib/seo-content.mjs` (how the generator and the discoverability verifier read that file); `src/app/pages/seo-content/seo-content.page.ts`; `scripts/generate-seo-pages.mjs`; `scripts/audit-seo-pages.mjs`; `scripts/check-route-sitemap-coverage.mjs` (every router route is in the generated sitemap or excluded with a written reason, every published route also declares `data.seo` so it is indexable at runtime, and a title or description written in both files must agree); `scripts/check-public-asset-shadowing.mjs` (nothing in `public/` shadows a file the generator owns - a committed 7-URL `sitemap.xml` sat beside the generated 4,638-URL one); `scripts/verify-guide-discoverability.mjs`; `scripts/public-entry-synthetics.mjs`; `scripts/pwa-shell-check.mjs`; `e2e/public-entry-visual.spec.ts`; `.github/workflows/deploy-pages.yml`; `.github/workflows/guide-discoverability.yml`; `.github/workflows/public-entry-synthetics.yml` | Route/unit coverage through `src/app/app.routes.spec.ts`, and `src/app/pages/seo-content/seo-content.page.spec.ts` builds each tool and guide page against the real route table; SEO audit script runs in `npm run build:pages`; `npm run test:route-sitemap-coverage` proves the router and the sitemap generator still agree, and is proven red on a broken tree rather than only green on a correct one; `npm run test:public-asset-shadowing` proves exactly one file can be served as the sitemap; `npm run test:discoverability` covers guide inventory drift checks and the page-text reader; `npm run test:seo-pages` (lane `seo-pages`, 869f13c5c) runs the generator for real over one character of each shape and then the `build:pages` audit over that output, which now also requires each character page to lead with its bridge, link no destination twice, and never print Rumble stats as a passive or a special as its cooldown alone; `sitemap.html` has a stated purpose, written above `writeSitemapHtml` - the static, JavaScript-free index of every public page that is not a character page, sectioned Home / Tools / Guides / App screens / Help and legal, plus the twelve newest characters - and the audit fails if a section or a public page is missing (869f13c5r); the how-to-build guide's *When your team keeps losing* section names only checks the app runs, and the how-to-build and Pirate Rumble guides each have an in-app entry point - Auto Team Builder's *How this page works* card and the Auto Team Rumble Builder's hero - held by the discoverability inventory's `appHelpHints` (869f13c5b); `npm run test:public-entry-synthetics` covers live monitor URL redaction, workflow fallback alignment, and failure classification; `npm run test:pwa-shell` covers service-worker update and cache-freshness assertions | `@post-merge-smoke` covers release-critical guide rendering; `Public Entry Synthetics` checks the deployed guide route and redacted share-link landing on schedule and when dispatched after successful Pages deploys on `main`; the `Deploy GitHub Pages` `cache-freshness` job verifies stale release A guide/share content before service-worker update activation and release B content after activation; `e2e/public-entry-visual.spec.ts` keeps Chromium desktop/mobile baselines for guide, tool-page and share-link first-viewport layout, and `src/app/pages/public-primary-action.spec.ts` holds every tool and guide page to one solid primary action (869f13c5p) | `npm run build:pages`; `npm run seo:audit`; `npm run routes:sitemap-coverage`; `npm run seo:public-assets`; `npm run discoverability:verify`; `npm run test:pwa-shell`; `npm run test:public-entry-visual`; `PUBLIC_ENTRY_BASE_URL=https://optcteambuilder.com npm run synthetic:public-entry`; Deploy GitHub Pages, Guide Discoverability, Public Entry Synthetics, and PWA cache freshness artifacts | `README.md`; public guide links in the live-site section; `../optc-team-builder-brain/audits/869dwcbb4-guide-discoverability.md`; `../optc-team-builder-brain/audits/869dwcbbd-public-entry-synthetics.md`; `../optc-team-builder-brain/audits/869dwcedz-public-entry-visual.md`; `../optc-team-builder-brain/audits/869dwchu2-pwa-cache-freshness.md` | SEO content, guide/help discoverability, Pages deployment, public entry visual baselines, public entry synthetics, and PWA cache freshness | Strong |
| Android release workflow | Manual `Release Android` workflow and local release skill | `.github/workflows/release-android.yml`; `scripts/release-and-tag.sh`; `scripts/bump-version.sh`; `scripts/release-provenance-report.mjs`; `scripts/post-dispatch-production-smoke.mjs`; `android/`; `src/app/core/services/android-back-button.service.ts` (869f6tcyk. The hardware back key on Home did nothing, because `@capacitor/app` hands every press to Ionic and no Ionic handler covered the root screen. One handler below Ionic's own now minimizes the app when no outlet has a page behind the current one); `capacitor.config.ts` and `android/app/src/main/res/values/styles.xml` (869f6tcz0. The system bars followed the phone - white bars around the dark-only app on a light device - until SystemBars was set to `DARK` and the theme's window background, which Capacitor paints behind both bars, to the app's own `#070b17`); `src/app/core/services/native-update.service.ts` and `android/app/src/main/java/com/john/optcteambuilder/ApkUpdaterPlugin.java` (869f6tczy. The in-app updater fetched the ~207 MB APK a second time when the player came back from allowing "Install unknown apps" or from a cancelled installer. The service now remembers the file and its version and installs it, the plugin checks the SHA-256 the release publishes before installing, and a missing or mismatched file is downloaded afresh) | `scripts/bump-version.spec.ts` covers the version scripts, including the two-digit segment cap; `src/app/core/services/android-back-button.service.spec.ts` covers the hardware back key (Home leaves the app, a menu screen or a character page goes back, an open modal or side menu keeps the key, and the web app is untouched); `scripts/generate-native-surface-system-bars.spec.ts` keeps the dark system bars in the native surface record; `src/app/core/services/native-update-downloaded-apk.spec.ts` covers the tap after a download (installs the file on disk, hands over the published digest, downloads afresh when the file is gone or mismatched, and prefers a newer release); the rest is covered through release workflow execution, provenance fixtures, post-dispatch smoke fixtures, and local release runbook checks | No browser E2E by default before dispatch; release commits trigger Pages deploy and the post-dispatch production smoke job checks the deployed guide/share-link shell | `npm run test:release-check`; Release Android workflow; `release-provenance` and `post-dispatch-production-smoke` artifacts; local `optc-release` skill; post-release `main` monitoring | `README.md`; `docs/post-dispatch-production-smoke.md`; `../optc-team-builder-brain/.codex/skills/optc-release/` | Native release, version, artifact provenance, and post-dispatch production smoke ownership | Covered |
| i18n namespace ownership | Every screen - the map covers all 27 translation namespaces | `docs/i18n-namespace-ownership.json` (generated); `scripts/generate-i18n-ownership.mjs`; `public/i18n/` | `scripts/generate-i18n-ownership.spec.ts` derives ownership from where each scope is referenced and pins the cross-namespace reads | No UI route; the map is read by maintainers and by the guard | `npm run i18n:ownership` checks it; `npm run i18n:ownership -- --write` regenerates it | `../optc-team-builder-brain/audits/2026-09-14-869f12x53-data-capture-wave-2.md` | Translation scope ownership and bilingual copy | Covered |
| Shared AND/OR tag-set filters | Tag filter modal on `/tabs/characters`, `/tabs/character-boxes`, `/tabs/captain-coverage`, `/tabs/manual-team-builder`, `/tabs/auto-team-builder` (team-wide requirement plus the requirement-source, manual-lock, and exclude candidate pickers), `/tabs/auto-team-builder-rumble` (manual slot picker), `/tabs/saved-teams`, `/tabs/rumble-characters`, and the shared character image picker | `src/app/shared/ability-tag-set-picker/ability-tag-set-picker.component.ts`; `src/app/shared/character-tag-set-picker/character-tag-set-picker.component.ts`; `src/app/shared/character-tag-filter/character-tag-filter.component.ts` (self-contained `characterTags` filter shell: trigger, chips, live region, and the nested picker); `src/app/core/services/ability-filter-tag-set.utils.ts`; `src/app/core/services/character-tag-set.utils.ts` (`normalizeCharacterTagKey`, `buildCharacterTagMatchIndex`, `matchesCharacterTagSets`, `resolveCharacterTagSetSelectionMatchingCharacterIds`); `src/app/core/services/special-ability-filter.utils.ts`; `public/i18n/character-tag-filter/` | `src/app/core/services/ability-filter-tag-set.utils.spec.ts` (golden replay against the legacy flat resolver); `src/app/core/services/character-tag-set.utils.spec.ts`; `src/app/shared/character-tag-filter/character-tag-filter.component.spec.ts`; `src/app/shared/character-tag-filter/character-tag-filter.i18n.spec.ts` (EN/EL leaf parity, placeholder parity, and distinct `or`/`and` joiners); both picker component specs; per-host coverage in each migrated page spec | `perf:ability-filters` and `perf:mobile-pickers` drive the modal open/pick/save round trip on Manual Team Builder, Saved Teams, and the character image picker; the character-tag hosts have no dedicated E2E yet and rely on host specs plus focused live checks | `npm run test:ci`; `npm run i18n:validate`; `PERF_ASSERT=0 npm run perf:ability-filters`; `npm run perf:mobile-pickers` | `docs/maintainer-validation-guide.md` (Character Tag Filters); `../optc-team-builder-brain/audits/issue-170-and-or-tag-set-filters.md` | Shared filtering UI and ability/tag match semantics | Covered |
| Shared multi-select type and class filters | `app-character-facet-filter` on `/tabs/characters`, `/tabs/character-boxes`, `/tabs/captain-coverage`, `/tabs/manual-team-builder` (candidate picker), and the shared character image picker (Crew Forge, Saved Enemies). Auto Team Builder, Auto Team Builder Rumble, and Pirate Rumble keep their own single-value type/class controls and are deliberately out of scope: their type/class inputs are requirement pools fed to the build engine, where "all" would mean one character that is simultaneously two types. | `src/app/shared/character-facet-filter/character-facet-filter.component.ts` (flat value list plus one `'all' \| 'any'` match mode; `presentation="chips"` for the 5 types, `presentation="select"` plus a removable chip row for the 10 classes); `src/app/core/services/character-facet-filter.utils.ts` (`normalizeCharacterFacetSelection`, `matchesCharacterFacet`, `buildCharacterFacetSqlClause`, `toDetailedQueryFacetFields`, `CHARACTER_TYPE_LIKE_CLAUSE`, `CHARACTER_CLASS_LIKE_CLAUSE`); `src/app/core/services/optc-repository.service.ts`; `src/app/core/services/character-catalog-cache.service.ts`; `public/i18n/character-facet-filter/` | `src/app/core/services/character-facet-filter.utils.spec.ts` (exhaustive predicate/SQL parity tables over every type subset and both stored orders of every dual-type pair); `src/app/core/services/optc-repository.service.spec.ts` (SQL branch vs override branch differential); `src/app/core/services/character-catalog-cache.service.spec.ts`; `src/app/shared/character-facet-filter/character-facet-filter.component.spec.ts`; `src/app/shared/character-facet-filter/character-facet-filter.i18n.spec.ts` (EN/EL leaf parity; facet values are never translated); per-host coverage in each of the five host specs | No dedicated E2E yet. Hosts rely on their page specs plus the manual checks in the validation guide; the mobile layout of the chips row and the Greek `mode.*` copy are live-only checks. | `npm run test:ci`; `npm run i18n:validate`; `npx tsc -p tsconfig.spec.json --noEmit` (host specs are typechecked only there and by `ng test`) | `docs/maintainer-validation-guide.md` (Type and Class Facet Filters); `docs/copy-review-checklist.md` | Shared filtering UI and character facet match semantics | Covered |

## Maintenance Rule

When a PR adds, removes, renames, or materially changes a flow, validation
harness, workflow, release report, artifact path, or maintainer command, update
this map in the same PR. Prefer strengthening an existing row over adding a
parallel note. If a flow remains `Partial`, describe the missing automated or
live ownership path in the row instead of creating a broad follow-up task.
When a mapped flow's source path or docs owner changes, update
`docs/docs-drift-map.json` in the same PR so the drift guard keeps matching the
coverage map.

## Ability Catalog Audit Coverage

The derived ability catalog (`public/assets/data/optc-auto-builder-abilities.json`,
built by `scripts/auto-team-builder-ability-parser.mjs` from the definition files in
`scripts/data/`) is audited **one effect at a time** rather than as a whole. Each of the
five ability types — captain, special, crewmate, potential, support — has a coverage
ledger in the brain repo under `optc-team-builder-brain/audits/ability-audits/`, and the
shared procedure lives in `optc-team-builder-brain/scripts/optc-ability-audit/METHODOLOGY.md`.

An audit is not complete until it has covered both halves:

- **Detection** — reconcile the parser's matches for the effect against the raw ability
  text of every character, using the exact wording OPTC-DB uses upstream (which is
  frequently not the community or in-app name). The verdict is one of `verified-correct`,
  `fixed`, `corrections-added`, `no-data` or `schema-limited`, and it is recorded with
  before/after match counts and the sources behind it.
- **Selection surface** — reconcile how the effect can be *picked* in the app: whether a
  turn-count control should appear (only when the effect's real data carries a turn value),
  and which of the four team-role scopes (captains / subs / crew / self) are genuinely
  populated, since a scope with zero matching characters must not be offered.

The conditional "boost against <enemy state>" family (`boost_against_delayed_enemies`,
`boost_against_def_reduced_enemies`, `boost_against_poisoned_enemies`) recognises two clause
shapes, and all three now read both — the poison key additionally covers the Toxic-only object
form (`boosts ATK against enemies inflicted with Toxic`), since Toxic is a genuine poison tier: the canonical participle form `boosts ATK against delayed enemies by Nx`, and the
2024+ status-noun list form `boosts damage dealt to enemies inflicted with Increase Damage
Taken, Delay, Poison, ..., DEF Reduction, or Paralysis by Nx`. The governing frame
`boosts ... (against|damage dealt to) ... enemies (inflicted with|affected by)` is
load-bearing: it keeps out the applier (`delays all enemies by N turns`, `apply_delay`), the
enemy immunity (`Delay Debuff Protection`), the amplifier (`increases boost effects of Delay
Status ATK Boost buffs`, `effect_boost`) and the one-shot trigger form (`If there are delayed
enemies when the Special is activated, ...`), none of which is a per-hit boost against the
state.

Turn requirements distinguish a *complete removal* from a long duration. Wording like
`removes enemies' Poison duration completely` carries the `minTurns: 99` sentinel, but 99 is
not a safe discriminator, because upstream also writes literal `for 99+ turns` and `for 999
turns` for genuinely long effects. The catalog therefore emits `completeRemovalCharacterIds`
(and a captain-scoped twin) for the 20 keys that have them, and the turn filter unions those
ids into every turn-filtered result instead of comparing them numerically — so a permanent
cure satisfies any requested turn count, while a real 99-turn boost still drops out of a
"100 turns" request.

The ability requirement picker shows a match count per catalog tile, and it is
SOURCE-AWARE: in captain mode it reports the captain-scoped count rather than the
ability-wide one, because a captain requirement resolves against
`captainAbilityMatchingCharacterIds`. The two can diverge sharply — "Enemy Resilience"
matches 137 characters overall but only one as a captain — so the ability-wide number would
promise matches the filter never returns. Counts at or below five are styled as scarce to
warn that the requirement will likely over-constrain the search, without disabling it.

Duration-reduction clauses may chain a second effect without repeating the verb, and upstream
punctuates that continuation two ways — with a comma (`... duration by 5 turns, crew's ATK
DOWN duration by 5 turns`) and with a bare conjunction (`... duration by 5 turns and Barrier
duration by 1 turn`). The shared `TURN_PATTERNS` continuation accepts both, and each
continuation carries its OWN turn count rather than inheriting the first clause's. The
alternation is deliberately explicit rather than an optional comma: allowing any target to
follow a bare `turns ` would re-open the clause-bridging that the surrounding guards exist to
prevent.

Picker labels are searched alongside the key, so a label that does not use the in-game wording
makes an effect unfindable. `remove_chain_coefficient_reduction` is labelled
`Chain Coefficient Reduction (Decrease Chain Multiplier Growth Rate)`: the primary is the
name OPTC-DB uses identically for both the enemy application and the crew cure, and the
parenthetical preserves the legacy app wording for discoverability. The legacy wording alone
was actively misleading — it appears nowhere in the ability text, while its three-word core
names the OPPOSITE mechanic, the crew boost `chain_multiplier_growth_rate`. The cure must
also not be confused with `remove_chain_multiplier_limit` (the ceiling debuff's cure) or
`chain_multiplier_lock` (the crew fixed-value buff).

**Enemy-scoped duration removal.** `reduces/removes enemies' <X> duration` strips a buff from
the ENEMY; it is not a crew cure. Because `normalizeTargetText` strips the `enemies'`
possessive before target aliasing, the two are indistinguishable at the alias layer, so the
`TURN_PATTERNS` consumer recovers ownership from the raw target: a key is treated as
enemy-owned only when EVERY segment resolving to it carries the enemy possessive, and
enemy-owned segments are skipped for keys outside `ENEMY_TARGETED_REMOVAL_ABILITY_KEYS`. The
check is per-segment with a sticky owner rather than whole-target, so a mixed list
(`enemies' Percent Damage Reduction and crew's Chain Coefficient Reduction`) keeps both halves
with each going to its correct key.

**Super-special grants.** Matchers do not run over super-special text by default, because most
units repeat their base special's wording there and would be double-tagged. Effects whose grants
appear *only* in super-special text are opted in individually via a per-key allowlist, which
now holds six keys: `territory`, `chain_multiplier_growth_rate`, `restore_advance_special_charge`,
`apply_resistance_reduction`, `additional_damage_boost` and `final_tap_atk_boost`. Membership is
decided per key by measurement, not assumed — `boost_rcv` was tested and rejected, because every
one of its super-special hits was an orb token rather than a stat grant. The allowlist must stay per-key: removing
it so every matcher reads super-special text was measured to move 37 keys and add 577 matches.
For "Chain Multiplier: Growth Rate" specifically, ten units grant the buff only in their super
special and none repeats it in the base special, so no double-tagging occurs. Wordings that
*reference* a buff rather than granting it — duration extenders, boost-effect amplifiers and
trigger conditions — stay excluded on every source.

The three chain debuffs and the three chain buffs are separate mechanics and must keep
separate labels. `remove_chain_multiplier_limit` ("Chain Multiplier Limit") cures the enemy
CEILING on the chain multiplier; `remove_chain_coefficient_reduction` cures the enemy shrink of
its per-tap GROWTH; `chain_multiplier_lock` ("Chain Multiplier: Lock") is the crew buff that
fixes the multiplier at a value, alongside the additive and growth-rate boosts. Japanese uses a
single word for both the enemy cap and the crew lock, so translated sources appear to merge
them — follow the English split. The buff-extension wording `increases duration of any Chain
Lock/Limit/Boundary buffs` grants none of these; it belongs to `extend_turn_duration`.

Special-cooldown effects split four ways and must not be conflated. `reduce_special_charge` is
plain cooldown reduction; `reduce_ship_special_charge` is the ship's own separate cooldown;
`restore_advance_special_charge` covers both recovery from the enemy **Special Rewind** debuff
(which pushes the charge gauge backwards — every "restores ... when they are rewinded" clause)
and proactive self-charge ("advances Special Cooldown of this character to MAX"). The label
carries "Special Rewind Recovery" because the rewind mechanic drives most of that key's members
and the bare label named neither it nor the debuff. Ship clauses are held out of the crew keys
by an explicit guard.

**Resistance Reduction** (`apply_resistance_reduction`) is an enemy debuff lowering the enemy's
resistance to a specific Type (`[STR]/[DEX]/[QCK]/[PSY]/[INT]`) or Class (Fighter, Slasher,
Striker, Shooter, Free Spirit, Cerebral, Powerhouse, Driven), which raises the damage crew of
that Type or Class deal. It is one mechanic with two scope axes, not several. Canonical wording
is `reduces enemies' <Type/Class> Resistance by -N% for M turn(s)`, detected from special,
captain and super-special text. The inverse (`increases enemies' ... Resistance`, a
self-inflicted drawback) is deliberately excluded, as is the crew-side immunity family, which
lives in potential abilities under Status Effect Immunity.

**Additional Damage Boost** (`additional_damage_boost`) matches only the *grant* —
`adds Nx character's ATK as Additional [Typeless] Damage`, plus the health-loss basis
(`adds Nx the damage taken from enemies before the special is activated ...`). The amount may
be decimal, so both gaps use the decimal-tolerant construct. Preconditions, duration extenders,
replace-triggers, buff-list references and the adverbial `by an additional 1.2x` are all
excluded — they reference the buff without granting it. It is distinct from
`end_of_turn_additional_damage` despite the near-identical name (a buff on your crew's attacks
versus the character dealing a tick); seven characters legitimately carry both, from separate
clauses in different source fields.

**Boost RCV** (`boost_rcv`) matches only crew RCV **stat** grants — `Boosts RCV of ...`,
`Boosts ATK and RCV of ...` — in both multiplier (`by 2x`) and flat (`by 45`) form, and it
exposes a turn filter like its twin `boost_atk`. RCV is also an orb colour, so the matcher
explicitly excludes the `[RCV]`-orb senses (drop-rate boosts, beneficial-orb clauses, orb
changes, orb-count conditions and orb-heal amplification), plus RCV-scaled heals
(`recovers Nx character's RCV in HP` → Heal HP) and RCV DOWN cures. Note the decimal-tolerant
gap construct used elsewhere in the parser is deliberately **not** used here: for this key the
plain gap acts as a firewall, and widening it admits false positives with no genuine gains.

**Poison — inflict versus cure.** `inflict_poison` ("Inflict Poison", *Apply Status Effect*)
covers crew→enemy infliction and folds all four tiers together: Poison, Strong Poison (Venom),
Toxic (Progressive Poison) and Reiju Poison. `remove_poison` ("Poison", *Reduce Status Effect
Duration*) is the crew-side cure and keeps the bare name, because that reproduces upstream's
own cure-filter name. Until this audit both shipped the bare label "Poison" — the only label
duplicated inside a single category — so a mis-pick returned a nearly disjoint character set.
The matcher requires the finite verb (`Poisons ... enemy/enemies`) rather than the noun, which
is what keeps cures, crew-side preconditions and the immunity-piercing enabler
(`allows effects that inflict Poison to ignore Debuff Protection`) out.

**Captain Swap** (`swap_captains`) exchanges a crewmate into the Captain slot for N turns, so
that unit's Captain Ability applies to the crew. It is symmetric — bosses inflict it as
sabotage and crew specials grant it as a tool — and the status is written "Captain Swap" on
both sides, so the label carries no alias. Detection reads `swapData` in addition to the normal
sources, but only through a **post-filter to this single key**: `swapData` is not a general
ability source, and feeding it through the analyser unfiltered was measured to move 40 keys and
add 564 matches, because the duration-reduction patterns run above the source gate. The cure
(`removes Captain Swap duration completely`, 34 carriers) is a distinct upstream effect that
currently belongs to no key.

Where a status exists on both sides of the board, the cure and the application are separate
keys and must stay separately named. `remove_increase_damage_taken` ("Remove Increase Damage
Taken") is the crew-side cure; `apply_increase_damage_taken` ("Increase Damage Taken") is the
enemy-side application. A bare shared noun is not an option here: the picker's selected-rows
panel shows only a badge and the label, and the badge is derived from the label's first two
words, so two keys sharing a noun render identically with no way to tell them apart.

**Normal Attack Only** (`ignore_normal_attack_only`, "Ignore Normal Attack Only (NAO)") is a
crew-side status ailment applied by enemies: every damage instance belonging to a skill —
special, percent, fixed and end-of-turn follow-up — is floored at 1, while specials still fire
and their non-damage effects still work. Only normal attacks keep dealing real damage. The key
tags the *counter*: units whose own damage clause ignores or bypasses the ailment. Both upstream
names resolve to it, the original and the newer "Non-Normal Attacks Deal 1 Damage". Note the
label previously read "Ignore DEF/Defensive Effects", which names two different clauses — in
this game's wording "Fixed" carries ignore-DEF and "True" carries ignore-defensive-effects, so
those belong to `deal_fixed_damage` and to the damage wording itself, not here.

**Weaken** (`apply_weakened`) is an enemy-side damage-amplification debuff the crew inflicts,
distinct from Increase Damage Taken and explicitly *conditioned* on it (`inflicts all enemies
with Weaken by 1.5x, by 1.875x instead if enemies are inflicted with Increase Damage Taken`) —
an effect cannot be conditioned on itself, which is what separates the two. It is matched on the
verb **plus its object** rather than on the debuff name, because the corpus also contains a
debuff-protection pierce enabler using the same verb (`allows effects that inflict Increase
Damage Taken and Weaken to ignore Debuff Protection`); only requiring `enem(y|ies) ... with`
between verb and name keeps it out. There is no cure key and none is warranted: the single strip
clause is an enemy-owned self-cleanse before re-application.

**RCV DOWN** (`remove_rcv_down`) is an enemy-applied debuff that slashes the crew's total RCV
stat to a near-floor value, so meat orbs and RCV-scaled heals recover almost nothing. The cure
grammar is `reduces|removes ... RCV DOWN ... duration (by N turns | completely)`. The `down`
token is load-bearing: `Counter-RCV` and `Counter-Healing` are separate upstream mechanics with
no key today, and a bare `rcv` alias absorbs all nine Counter-RCV units. Upstream folds
`selected debuff(s)` into its own RCV DOWN matcher; we deliberately route that wording to
`remove_pain` instead, so `remove_pain` staying at 16 is the canary for this alias over-reaching.

The sibling `remove_rcv_bind` is deliberately kept at zero rather than deleted. RCV Bind is a
**real** mechanic, but it exists only in Pirate Rumble and Grand Party — every occurrence is in
`rumbleData`, which no special-ability source reads. Keeping the key also documents a latent
leak: `remove_bind` matches any target ending in " bind", so an "rcv bind" target would be
silently miscounted as plain Bind if upstream ever ports the Rumble status into quest text.

## Special-ability audit batch (2026-07-22)

A full pass over the remaining un-audited **special** effects revived six dead keys — each had a
matcher keyed on a wording OPTC-DB never uses — and hardened two more:

- **Chain Boundary** (`chain_multiplier_lock_min_max`, 0 → 118) sets a floor/ceiling on the chain
  multiplier (OPTC-DB *Chain Limit: Boundary*, Lower/Upper Bound). Grant wording is `sets Chain
  Boundaries to <min>x and <max>x for N turns`; the old matcher looked for the non-existent
  "minimum/maximum chain multiplier". Distinct from `chain_multiplier_lock` ("locks the chain
  multiplier at Nx"); the `sets` verb keeps effect_boost and condition-list references out.
- **DEF Reduction** (`apply_def_reduction`, 0 → 259) is the crew applying DEF Down to enemies,
  spelled out as `reduces the defense of all enemies by N% for N turns` — never the abbreviation
  "DEF". The `by (N|?)%` anchor separates it from the Increased Defense *buff* removal
  (`remove_enemy_increased_defense`, a duration reduction).
- **Boost Orb Chance** (`change_slot_chance`, 0 → 227) raises the probability of specific orb
  colours: `boosts/increases [the] chance(s) of getting <orb> orbs` (chance→orb; the old matcher
  required orb→chance). The "lowers/reduces chances" drawback direction is excluded. Since
  869f13c62 it also reads the two spellings that missed 6 captains: the scoped `boosts chances of
  <class> characters getting <orb> orbs` (through a short gap that may not cross a comma or another
  effect verb) and `boosts/increases [the] rate of <orb> orbs`.
- **Critical Hit Rate / Damage** (`critical_hit_chance_boost` 0 → 10, `critical_damage_boost`
  0 → 15) are the special/captain grants of the Critical Hit buffs — OPTC-DB names them "Critical
  Hit Rate" and "Critical Hit Damage", not the bare "Chance"/"Damage" the old matchers used.
  Distinct from the innate `potential_critical_atk`.
- **VS switch reductions** (`reduce_switch_effect_use` 0 → 89, `reduce_vs_effect_gauge` 0 → 48)
  let a VS/dual unit switch forms sooner: `reduces [the] Switch Effect of <scope> by N turns` and
  `reduces ... VS Gauge of <scope> by N` (often the shared clause "Reduces Switch Effect and VS
  Gauge of all characters by N"). Since 869f13c62 the switch window is 80 characters and the gauge
  also reads `VS Effect gauge`, the spelling the first Japanese-derived unit (#4647) uses.
- **Ship Special Cooldown** (`reduce_ship_special_charge`) reads `reduces` **and** `advances ...
  Special Cooldown of Ship` since 869f13c62: five captains charge the ship's special to MAX, and no
  other key found them. **Nullify Damage** (`nullify_damage`) stops at another effect verb, so a
  70% cut followed by "reduces the defense of all enemies by 100%" is no longer read as a nullifier.

`apply_set_target` (26 → 28) and `class_change` (10 → 19) additionally gained **superSpecialText**
sourcing via the per-key allowlist, for units that carry the effect only in their super special.
Seven further keys were verified correct with no change, and four (`apply_unique_effect`,
`boost_max_hp`, `chain_multiplier_multiplicative_boost`, `remove_beneficial_effect`) are genuine
no-data placeholders whose matchers are correct but whose effect no current unit produces.

On the Characters (Vault) page, an unrelated UI fix drops the search bar below the facet /
favorite / sort controls on phones (`max-width: 640px`) so it sits directly above the character
grid; the desktop search-first single-row layout is unchanged.

Two conventions come out of this and apply to any new or relabelled effect:

- Where an effect is named differently depending on who applies it, or where the community
  name differs from the ability-text wording, the label carries both as `Primary (Alias)` —
  see `docs/data-schemas.md`. Current examples: `Boost Type Effects (Color Affinity)`,
  `Tap-Timing Requirement (PERFECT)`, `Protect from Defeat (Resilience)`.
- Every parser change is proved targeted by a semantic diff of the regenerated catalog
  against `HEAD`, showing that only the intended key's match data moved.

Validation: `npm run test:ci` (the ability parser spec carries a regression case for each
audited wording), `npm run test:captain-contracts`, `npm run test:dataset-digest`, and
`npm run perf:dataset -- --assert`. Ledger status comes from the ledger tool at
`optc-team-builder-brain/scripts/optc-ability-audit/ledger.py`, invoked from the brain repo
with the ability type and the `status` subcommand.

## Support-ability audit batch (2026-07-22)

A full pass over the remaining un-audited **support** effects (source `supportData`) closed the
board at 67/67 — 44 keys corrected, 15 verified correct, 6 genuine no-data. Highlights:

- **Base-stat grants** (`base_atk_boost` 824 → 317, and `base_atk/hp/rcv_boost_additional`
  +507 each): OPTC-DB writes the three-stat grant as the Oxford list `base ATK, HP and RCV`,
  but the stat-list regex only traversed ` and ` separators, so it captured just `ATK` and mis-filed
  the grant under the single-stat key. The separator now accepts a comma, so a multi-stat grant
  emits the `_additional` variant for every stat it names.
- **Applied status effects** (`apply_status_effect_def_down` 0 → 22, `_reduce_resistance` 0 → 21,
  `_delay` 0 → 5, `_increased_damage_taken` 3 → 6, `_poison` 4 → 5): these are always granted
  `for N turns`, so they were nulled by the shared duration/turns guard on the apply-effect list.
  Each now has a dedicated pre-gate check keyed on the crew→enemy action clause (e.g. `reduces the
  defense of all enemies by N% for M turns`, never the abbreviation "DEF Down").
- **Status-effect recovery** now uses the OPTC-DB debuff names: *Chain Coefficient Reduction*
  (`_reduce_chain_multiplier_growth_rate` 0 → 16), *Chain Multiplier Limit*
  (`_lock_chain_multiplier` 0 → 12), *Silence* = Special Bind (`_special_bind` 40 → 41), and the
  `Increase Damage Taken duration` cure (`_increased_damage_taken` 0 → 4). The poisons cure is
  anchored on `<poison> duration` so a poison-offense clause is not read as a cure.
- **Enemy-effect duration reductions** are keyed off the reduce action, and a trailing `[AUTO+]`
  auto-ability clause (a trigger, not the support's own action) is stripped before matching — so
  a "DEF Up" named only in `[AUTO+] When enemy launches DEF Up status` (#4600) no longer
  false-positives `reduce_enemy_effect_turns_def_up`, while a genuine reduction worded `by N turns`
  without the literal "duration" (#4193) still matches. The `(except Threshold Damage Reduction)`
  carve-out routes #4600 to `_damage_reduction`, not `_damage_threshold`.
- **Damage boosts** now read `boosts ATK against delayed / poisoned enemies` (`damage_boost_delay`
  0 → 9, `_poison` 0 → 3, `_venom` 0 → 2 via the Toxic tier, `_def_down` 0 → 13) and the decimal
  `adds 0.Nx to Chain multiplier` grant (`chain_multiplier_boost` 15 → 44) that a bare `[^.]` gap
  had died on at the decimal point.
- **Other**: permanent vs turn vs threshold damage reduction disambiguated
  (`damage_reduction_permanent` 280 → 201, keyed on `reduces damage received from …` / flat
  `by N%`); designated-turn activation now recognises `when you reach the final stage`
  (`effect_activation_on_designated_turn` 4 → 188); and Special Cooldown reduction is scoped to
  self (`reduce_special_charge_time_self` 0 → 67 — `of (the) supported character`, deliberately
  excluding the Captain/Powerhouse/all-scope cooldown reductions that are not a `(Self)` effect).

Every fix was verified by a deterministic parser harness over the 2,235-text `supportData` corpus
(reproduces the shipped catalog exactly) plus the offline regen, whose catalog semantic diff was
support-only. Seven new regression cases in the parser spec pin the corrected wordings.
